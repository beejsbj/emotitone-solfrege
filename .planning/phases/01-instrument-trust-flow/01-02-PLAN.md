---
phase: 01-instrument-trust-flow
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/instrument.ts
  - src/types/instrument.ts
  - src/stores/music.ts
  - src/components/InstrumentSelector.vue
  - src/__tests__/stores/instrument.test.ts
  - src/__tests__/stores/music-live-notes.test.ts
  - src/__tests__/stores/music.test.ts
  - src/__tests__/components/ui/InstrumentSelector.test.ts
autonomous: false
requirements:
  - FEEL-02
  - FEEL-03
must_haves:
  truths:
    - User sees the newly selected instrument immediately and the UI admits when it is still warming.
    - Live note playback does not switch to a cold instrument before warming completes.
    - If two instrument selections happen quickly, the newer choice wins and stale warmup results do not overwrite it.
  artifacts:
    - path: src/stores/instrument.ts
      provides: Selected-vs-ready instrument state and race-safe warmup handling
    - path: src/types/instrument.ts
      provides: Instrument readiness state contract
    - path: src/stores/music.ts
      provides: Live note playback guard against unready instruments
    - path: src/components/InstrumentSelector.vue
      provides: Honest selected/warming/ready UI
  key_links:
    - from: src/components/InstrumentSelector.vue
      to: src/stores/instrument.ts
      via: selected and warming state
      pattern: aria-busy|warming|selected, not ready yet
    - from: src/stores/music.ts
      to: src/stores/instrument.ts
      via: ready/playable instrument selection
      pattern: readyInstrument|playableInstrument
    - from: src/stores/instrument.ts
      to: src/services/superdoughAudio.ts
      via: isPrewarmed and prewarmSoundSamples
      pattern: isPrewarmed|prewarmSoundSamples
---

<objective>
Make instrument switching honest by separating the selected instrument from the playable-ready instrument and exposing that readiness state to both audio flows and UI.

Purpose: Implement D-04 through D-07 so the app stops pretending a newly selected instrument is ready before its samples are warm.
Output: A race-safe instrument store contract, readiness-gated live-note playback that reports the actual playable instrument, an instrument selector that visibly communicates warming state, and a blocking browser verification for switching behavior.
</objective>

<execution_context>
@/Users/burooj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/burooj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-instrument-trust-flow/01-CONTEXT.md
@src/stores/instrument.ts
@src/types/instrument.ts
@src/stores/music.ts
@src/stores/patterns.ts
@src/composables/useMidiControls.ts
@src/components/UnifiedVisualEffects.vue
@src/components/InstrumentSelector.vue
@src/services/superdoughAudio.ts
@src/__tests__/components/visual/UnifiedVisualEffects.test.ts
@src/__tests__/stores/music-midi.test.ts

<interfaces>
From `src/stores/instrument.ts` today:

```ts
return {
  currentInstrument,
  isLoading,
  initializeInstruments,
  setInstrument,
}
```

From `src/stores/music.ts` live note paths:

```ts
await superdoughAudio.attackNote(noteId, noteName, instrumentStore.currentInstrument)
await superdoughAudio.playNoteWithDuration(noteName, durationMs, instrument || instrumentStore.currentInstrument)
```

From `src/stores/patterns.ts` listeners:

```ts
handleNotePressed(detail) expects:
  note, frequency, noteName, solfegeIndex, octave, noteId, instrument

handleNoteReleased(detail) expects:
  noteId
```

From `src/composables/useMidiControls.ts` listeners:

```ts
interface MirroredNoteEventDetail {
  source?: string;
  duration?: string;
  durationMs?: number;
  noteId?: string;
  noteName?: string;
  octave?: number;
  solfegeIndex?: number;
}
```

From `src/components/UnifiedVisualEffects.vue` listeners:

```ts
onNotePlayed(detail) reads:
  note, frequency, noteId, octave, noteName, mode, key

onNoteReleased(detail) reads:
  note, noteId
```

From `src/components/InstrumentSelector.vue`:

```ts
const currentInstrumentId = computed(
  () => props.currentInstrument || instrumentStore.currentInstrument
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a race-safe selected-versus-ready instrument contract</name>
  <files>src/stores/instrument.ts, src/types/instrument.ts, src/__tests__/stores/instrument.test.ts</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/01-instrument-trust-flow/01-CONTEXT.md
src/stores/instrument.ts
src/types/instrument.ts
src/services/superdoughAudio.ts
  </read_first>
  <behavior>
    - Test 1: Selecting an already prewarmed instrument updates selected and ready state immediately with no lingering warming state.
    - Test 2: Selecting a cold instrument updates the selected instrument immediately, enters a warming state, and only promotes the ready instrument after warmup finishes.
    - Test 3: If selection B starts while selection A is still warming, selection A cannot overwrite selection B when its promise resolves later.
  </behavior>
  <action>Per D-04 through D-07, expand `src/stores/instrument.ts` beyond `currentInstrument` and `isLoading`. Keep `currentInstrument` as the user-facing selected instrument to minimize UI churn, but add explicit ready-state refs such as `readyInstrument`, `warmingInstrument`, `isWarmingInstrument`, and a concrete status enum/string in `src/types/instrument.ts`. Add a monotonic request token or equivalent stale-request guard so only the most recent `setInstrument()` call can finalize ready state. `setInstrument(name)` should set the selected instrument immediately, start background warmup via `prewarmSoundSamples(name)` if needed, and only flip the ready/playable ref when the latest request completes. Create `src/__tests__/stores/instrument.test.ts` to lock this contract down.</action>
  <acceptance_criteria>
    - `rg -n 'readyInstrument|warmingInstrument|isWarmingInstrument|requestToken|instrumentStatus' src/stores/instrument.ts` returns matches.
    - `rg -n 'InstrumentReady|InstrumentStatus|readyInstrument|warmingInstrument' src/types/instrument.ts` returns matches.
    - `test -f src/__tests__/stores/instrument.test.ts` succeeds.
    - `bun run test:run src/__tests__/stores/instrument.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>bun run test:run src/__tests__/stores/instrument.test.ts</automated>
  </verify>
  <done>The instrument store exposes separate selected and ready state, and stale warmups can no longer win over newer selections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route live playback and selector UI through readiness state without breaking the shared note event contract</name>
  <files>src/stores/music.ts, src/components/InstrumentSelector.vue, src/__tests__/stores/music-live-notes.test.ts, src/__tests__/stores/music.test.ts, src/__tests__/components/ui/InstrumentSelector.test.ts</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/01-instrument-trust-flow/01-CONTEXT.md
.planning/codebase/CONCERNS.md
src/stores/instrument.ts
src/stores/music.ts
src/stores/patterns.ts
src/composables/useMidiControls.ts
src/components/UnifiedVisualEffects.vue
src/components/InstrumentSelector.vue
src/services/superdoughAudio.ts
src/__tests__/stores/music-live-notes.test.ts
src/__tests__/stores/music.test.ts
src/__tests__/stores/music-midi.test.ts
src/__tests__/components/visual/UnifiedVisualEffects.test.ts
src/__tests__/components/ui/InstrumentSelector.test.ts
  </read_first>
  <behavior>
    - Test 1: When the selected instrument is still warming, `attackNote()` does not call `superdoughAudio.attackNote()` and leaves live note state unchanged.
    - Test 2: Once ready, live-note and duration playback use the ready instrument in both audio calls and dispatched `note-played`/`note-released` payloads.
    - Test 3: `InstrumentSelector` renders a visible warming state with `aria-busy="true"` and distinct selected-versus-ready copy while warmup is in progress.
    - Test 4: The payload fields consumed by pattern capture, visuals, and MIDI listeners remain stable after the readiness gate is introduced.
  </behavior>
  <action>Per D-05 through D-07, update `src/stores/music.ts` so live-note entry points (`playNoteWithFormat`, `attackNoteWithFormat`, and `playNoteWithDurationFormat`) consult the new instrument-store readiness contract instead of blindly reading `instrumentStore.currentInstrument`. Before changing any dispatch path, audit the current consumers in `src/stores/patterns.ts`, `src/composables/useMidiControls.ts`, and `src/components/UnifiedVisualEffects.vue`, then preserve their existing payload expectations: `note`, `frequency`, `noteName`, `solfegeIndex`, `octave`, `noteId`, `mode`, `key`, `instrument`, and duration/source fields where already emitted. Guard live-note paths so a cold selected instrument does not produce ambiguous first taps; on a warming selection, return early without mutating active-note state and without emitting partial note events. Ensure emitted note payloads report the actual ready/playable instrument, not just the selected one. In `src/components/InstrumentSelector.vue`, show the selected instrument immediately but render honest status UI with concrete copy such as `warming…`, `selected, not ready yet`, and `aria-busy="true"` while the ready instrument lags. Update the existing selector and music-store tests to cover the warming path, ready path, and unchanged consumer-facing event shape explicitly.</action>
  <acceptance_criteria>
    - `rg -n 'readyInstrument|isWarmingInstrument|selected, not ready yet|warming' src/stores/music.ts src/components/InstrumentSelector.vue` returns matches.
    - `rg -n 'aria-busy' src/components/InstrumentSelector.vue` returns one match.
    - `rg -n 'note-played|note-released' src/stores/music.ts` still returns the shared event dispatch points after the gating logic is added.
    - `bun run test:run src/__tests__/stores/music-live-notes.test.ts src/__tests__/stores/music.test.ts src/__tests__/components/ui/InstrumentSelector.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>bun run test:run src/__tests__/stores/music-live-notes.test.ts src/__tests__/stores/music.test.ts src/__tests__/components/ui/InstrumentSelector.test.ts</automated>
  </verify>
  <done>Instrument selection and playback tell the same truth: the user can see the chosen sound immediately, but nothing claims to be playable until the ready contract says it is.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify real browser instrument warming and switching behavior</name>
  <files>src/stores/instrument.ts, src/stores/music.ts, src/components/InstrumentSelector.vue</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/01-instrument-trust-flow/01-CONTEXT.md
src/stores/instrument.ts
src/stores/music.ts
src/components/InstrumentSelector.vue
  </read_first>
  <action>Use the built readiness contract in a real browser session before moving on. This checkpoint exists because unit tests can prove store-state transitions and emitted payloads, but they cannot prove that actual sample warming, honest UI status, and rapid instrument switching feel coherent in the browser. Confirm that the selected instrument updates immediately, warming state stays visible until the instrument is actually ready, taps during warming do not create misleading cold-sound behavior, and a rapid second selection wins over the first stale warmup.</action>
  <acceptance_criteria>
    - The selector updates to the newly chosen instrument immediately while also showing a concrete warming state.
    - While warming is in progress, the instrument surface does not silently behave as if the new instrument were already ready.
    - When warming completes, the same selected instrument becomes playable without needing to reselect it.
    - If two cold selections happen back-to-back, the second selection is the one that ends up ready/playable.
  </acceptance_criteria>
  <what-built>Selected-versus-ready instrument state in `src/stores/instrument.ts`, readiness-gated live playback in `src/stores/music.ts`, and honest warming status in `src/components/InstrumentSelector.vue`.</what-built>
  <how-to-verify>
    1. Run `bun run dev` and open the app in a fresh browser tab.
    2. Pick an instrument that has not been warmed in this session, and confirm the selector changes immediately while showing warming copy and a busy state.
    3. Try playing during warmup and confirm the UI still reads as not-ready rather than pretending the new instrument is already playable.
    4. Wait for warmup to finish and confirm the selected instrument becomes playable without another selection click.
    5. Refresh, then quickly choose one cold instrument and immediately choose another; confirm the second choice is the one that remains selected and eventually becomes ready/playable.
  </how-to-verify>
  <verify>
    <automated>bun run test:run src/__tests__/stores/instrument.test.ts src/__tests__/stores/music-live-notes.test.ts src/__tests__/stores/music.test.ts src/__tests__/components/ui/InstrumentSelector.test.ts</automated>
  </verify>
  <done>Real browser verification confirms that instrument selection, warming status, and eventual playability tell the same story instead of diverging between store logic and actual runtime behavior.</done>
  <resume-signal>Type "approved" if the warming flow behaves as described, or describe what broke in the browser.</resume-signal>
</task>

</tasks>

<verification>
- Run the new instrument-store test and the updated music and selector tests.
- Complete the blocking browser checkpoint for real instrument warming/switching behavior before marking the plan done.
- Run `bun run type-check` after both implementation tasks complete so the readiness contract is consistent across the store and UI.
</verification>

<success_criteria>
- `instrument.ts` exposes explicit selected and ready state.
- `music.ts` no longer routes live note playback through an unready instrument.
- `InstrumentSelector.vue` shows honest warming feedback instead of silently swapping to a cold sound.
- Manual browser verification confirms the real warming flow matches the planned selected-versus-ready contract.
</success_criteria>

<output>
After completion, create `.planning/phases/01-instrument-trust-flow/01-02-SUMMARY.md`
</output>
