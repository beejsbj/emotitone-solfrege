# Design Log

Chronological receipts for the current EmotiTone production visual-design pass. This is evidence, not current truth; `DESIGN_SYSTEM_TRACKER.md` supersedes older rows when architecture or sequencing changes.

Design-lab-era extraction and audit history remains recoverable in Git through `85c63fd` and the deleted ledger files. It is historical reference, not authority for this pass.

| Date | Gate | Receipt | Evidence / next |
| --- | --- | --- | --- |
| 2026-05-27 | Doctrine correction | Burooj rejected the apparent design-lab finish: more visual-system work remained; CodeStrip is unique; brand colors are decorative; brass is instrument metal; semantic color aliases are legacy cleanup; Sticker badge is brass-only; filled surfaces are preferred and borders minimized | `85c63fd`; begin user-led unit definitions before production migration |
| 2026-08-12 | Review workflow | Style guide established as a user-led interview/inspection surface; external design repo is reference-only; no autonomous design-lab or extraction | `BJS-35`; `ce937f6` restores production at `/` and guide at `/style-guide/` |
| 2026-08-18 | Note definition and formalization | Accepted Note as the controlled noninteractive owner of musical identity/presentation, including geometry, proportion, surface, labels, runtime color, and `sounding` | `2178916`, `25dac1a`, `32a7dc7`, `9b0a688`; next consumer Key |
| 2026-08-19 | Key formalization | Accepted and formalized Key as the native momentary interaction wrapper around complete Note | `c81558d`, `e67ff69`, `1df13bc`; keep routing/audio/store concerns outside Key |
| 2026-08-19 | Note + Key production adoption | Production Keyboard consumes accepted Key -> Note; legacy `KeyboardKey.vue` removed while current config, audio, haptics, stores, MIDI, and QWERTY were preserved | `57bc6dd`; Keyboard remains a separate definition gate |
| 2026-08-25 | Keyboard definition and workbench | Grilling concluded, then production `Keyboard.vue` became the single source and the guide mounted it through inert controlled usage. Exact visual density remains unaccepted | `52267c3`, `3e4690d`, `ebb0014`, `8038665`, `a301139`; inspect the matrix before acceptance |
| 2026-08-25 | Mission and separation | Made production visual migration with inspectable lineage explicit; compositions are production surfaces; separated glissando and other functionality from visual acceptance | `3de77e5`, `7f283de`, `89eed59` |
| 2026-08-25 | Music Color drift recovery | Recorded that `b140654` changed the unaccepted visual presentation from segmented wheel/octave 0–8 to swatches/octave 2–8 while consolidating runtime calculation authority | `7614537`, `694e8d4`; later independent Music Color gate |
| 2026-08-26 | Knob definition | Accepted one production-led Ring/Arc family, per-consumer Brass/Ivory, real role/state geometry, full-circle Boolean/Button, and preserved production interaction/motion | `0c6b3ff`; gallery framing excluded |
| 2026-08-26 | Knob formalization and adoption | Shared accepted visuals reached production and real specimens with focused tests, type-check/build, and live DOM/motion evidence | `c14c8d0`, `21659fc`; screenshot/recording unavailable and not claimed |
| 2026-08-28 | Knob architecture correction | Replaced split visual/behavior authority with one public deep module; every production and specimen caller and regression test now uses `components/primatives/Knob/index.vue`; preserved accepted visuals and production behavior/motion | `5da92b2`, `a6c55ed`; 17/17 focused tests, type-check/build, live Chrome production + Ring/Arc captures, positive browser horizontal-scroll handoff, and active Button rotation sampling; preserve public-interface-only lineage |
| 2026-08-28 | Workflow reset | Created temporary `$emotitone-design-system`, made lineage a unit completion gate, limited visual HITL questioning, routed cold agents through root `AGENTS.md`, and reduced repository doctrine to this log plus the tracker | Skill repo `3b32179`, `13db170`; project cleanup `1c2267c`; next visual unit CodeStrip + action bar |
| 2026-08-28 | Doctrine-loss audit | Restored unsuperseded visual corrections before retiring the old ledgers and recorded Sticker's source/specimen contradiction without reopening its broader accepted definition | Historical correction `85c63fd`; bounded Sticker correction remains open |
| 2026-08-28 | Badge boundary clarification | Burooj confirmed Badge is brass-only and raised that its visual identity may be distinct enough to become its own primitive. The mismatch is between accepted identity and current colorable-variant code/specimen, not a contradiction in Burooj's intent | Keep Sticker's non-badge definition accepted; compare extract-versus-retain visually before changing the source |
| 2026-08-28 | Knob responsive visual correction | Recovered the pre-formalization Knob's visual weight without restoring gallery hero sizing or fixed production dimensions. The public wrapper now yields to its container; face stroke, dome, center content, glow, and type scale proportionally, while the PatternCard keep control retains an explicit local density override | `7ed0127`; 17/17 focused tests, type-check/build, live production plus Ring/Arc captures at desktop/390/320, exact narrow-host centering, and fresh lineage re-audit pass |
| 2026-08-29 | Button definition | Accepted one icon-only momentary Button as a punched-out circular paper chad: flat ink/ivory or intentional brass material, small hard offset collapsing on press, tone-matched loading, disabled stillness, and 32/40/48px plus contextual sizing. Persistent state remains Boolean Knob; former Knob Button actions move here | Definition sequence `23a6c4a` through `52111c9`; explicit consolidated acceptance received before implementation |
| 2026-08-29 | Button formalization and adoption | Created authoritative `components/primatives/Button.vue`, mounted its real specimen, adopted momentary production actions, removed both IconButton sources and Knob Button, and moved persistent Config state to native Boolean Knob semantics. The audit then hardened disabled stillness, valid Boolean markup, stale-prop cleanup, and a required accessible name across all 24 Button consumers | `5532155`, `62399ae`; 29/29 focused tests, type-check, production build, live production/style-guide DOM and computed-style inspection, and fresh bounded lineage re-audit pass. Full repository suite remains baseline-red outside this unit: 38 files/348 tests pass; 14 files/13 tests fail on existing GSAP mocks, stale imports, timeouts/environment assumptions, and unrelated utility expectations |
| 2026-08-29 | Button visual correction | Burooj identified the inset ring as an unwanted border, the flat brass as token drift, and live icons as too small with possible optical imbalance. Accepted correction removes the faux ring, restores canonical brass sheen/glow, makes the icon recognizable and centered, and expands the guide with all four brass finishes plus agent-selected full geometry-token studies | Live computed evidence found exact SVG-box centering but only about 7–10px rendered icon sizes; geometry studies remain explicitly specimen-only and do not reopen the circular production shape |
| 2026-08-29 | Button visual correction adoption | Removed the inset ring, made Button own a 60% centered icon, defaulted production brass to sheen + glow, exposed all four canonical brass finishes, and added guide-only tile/offcut/tab/rip/rounded recipes combining clip or radius, tilt, and shadow. The audit caught and the follow-up fixed disabled brass sheen motion | `297e00f`, `90b13f3`, `c1b7ab2`; 34/34 focused tests, type-check/build, live production + guide DOM/computed-style evidence, exact icon centering and material/geometry inspection, disabled brass stillness, and fresh bounded lineage re-audit pass; screenshot unavailable and not claimed |
| 2026-08-30 | Button feedback correction | Reduced the authoritative Button icon box from 60% to 50%, restored the circular face across all guide geometry studies by limiting them to rotation/depth tokens, and added a specimen-only ink-paper Button with a brass icon | `84585e9`; 13/13 focused tests, type-check/build, live guide computed-style evidence at exact 50% icon ratio plus `border-radius: 50%`/`clip-path: none`, and fresh bounded lineage audit pass; screenshot unavailable and not claimed |
| 2026-08-30 | Circle-native geometry definition | Burooj clarified that Button merely afforded a lower-layer idea: tile, offcut, tab, paper-rip, and rounded-stock disc silhouettes are foundational Geometry tokens, not Button variants. The token guide owns their direct specimens; the real Button guide consumes them through its existing CSS seam while production Buttons retain the default circle | Circle-native outer silhouettes replace the rejected inner-patch composition; broader production assignment remains a separate adoption choice |
| 2026-08-30 | Circle-native geometry formalization | Added five authoritative `--clip-disc-*` values to the shared token source, direct Geometry specimens, and real Button specimen consumption without changing Button's API, source, default circle, or production assignments | `0c065c1`; 9/9 focused Button tests, type-check/build, live computed-style inspection, exact temporary SVG render, and fresh bounded lineage audit pass; collaborative preview screenshot failed and is not claimed |
| 2026-08-30 | Button and circle-native Geometry closure | Replaced low-poly disc polygons with responsive curved shapes: localized offcut/tab/rip events on otherwise perfect circles, a wobbly uneven stock circle, restrained tile tension, and clean-circle fallbacks. Burooj accepted the result as nice enough and closed Button work | `f0be471`; 9/9 focused Button tests, type-check/build, live guide screenshot and computed styles, and fresh bounded lineage audit pass with no findings. Production preview remained at its loading gate, so no fresh action-bar screenshot is claimed; current source confirms Button defaults remain `50%` radius / no clip and only guide specimens assign disc tokens. Production assignment stays separate |
| 2026-08-30 | CodeStrip ownership definition | Accepted CodeStrip as LiveStrip's unique successor and `glyph` as a responsive member of `NoteProportion`: Note retains its complete surfaced visual language, the CodeStrip host controls scale and playback sequencing, and no public intermediate compound is introduced | `aaad14f`, `5e69514`, `9d6211c`, `2875906`, `80afb6d`, `77e17db`; compare glyph ratio and row density live before accepting the remaining visual definition |
| 2026-08-30 | CodeStrip glyph formalization | Burooj selected the 3:4 surfaced Note candidate and requested it 20% smaller. `glyph` is now an authoritative Note proportion; CodeStrip supplies a 27.2–33.6px responsive block scale, fills its host, and no longer leaks obsolete `shape="glyph"`. The real guide specimen imports that source and its cold hash link scrolls after Vue mount | `f6fc21a`; 13/13 focused Note + CodeStrip tests, type-check, and build pass with existing dependency/chunk warnings. Live Chrome screenshot and computed geometry verified 20.4×27.2px at the inspected narrow cell, 3:4, surfaced, and hash-aligned at 16px. Production LiveStrip adoption, playback fill, row density, and remaining states stay open |
| 2026-08-30 | Chord and CodeStrip temporal direction | Accepted Chord as a public compound with two planned consumers: CodeStrip compares fused and zero-gap clustered structures, while the future Keyboard chord row uses fused + symbol. One fused surface carries independently animated member-note color bands progressing bottom-to-top. Rest stays CodeStrip-local as Ink filled progressively with Ivory. Textual duration remains in the comparison alongside distance, bar, stacked, and hidden treatments | Definition only; no component source created. Next gate is a Chord workbench resolving responsive surface proportion, gradient order, identity treatments, and simultaneous/rolled/staggered articulation before implementation |
| 2026-08-30 | Chord definition workbench | Added a provisional public `Chord.vue` compound and real guide specimen for fused/clustered structure, symbol/member identity, compact/balanced/wide responsive candidates, and independently controlled bottom-to-top member fills. Clustered composes authoritative Notes; the guide alone owns the looping rolled-attack clock and Reduced Motion freeze | 5/5 focused Chord tests, type-check, build, and diff check pass. Live desktop/390/320 inspection found the matrix contained and the fused/clustered distinction legible; sampled hero progress changed independently over 320ms. No production adoption or final visual acceptance; inspect at `#compound-chord` next |
| 2026-09-01 | Chord visual and display correction | Removed the invalid independent structure/identity axes: symbol is always fused and notes are always clustered. The fused surface now consumes the shared Note paper sheen, Note/key shadow, and whole-surface geometry family; symbol type is larger. Progress starts at Ink and reveals unchanged runtime music colors with independently controlled transform fills, preserving octave lightness semantics | 18/18 focused Chord + Note tests, type-check, production build, and diff check pass. Live computed styles confirmed the 162x54 offcut hero, 30px symbol, Ink base, exact member colors, independent bottom-origin fills, shared paper sheen, and a real-Note cluster with no fused surface. Collaborative screenshot and fresh narrow resize failed and are not claimed. No production adoption or final visual acceptance; reinspect `#compound-chord` next |
| 2026-09-01 | Chord visual acceptance | Burooj accepted the corrected live Chord workbench. Chord is now defined, `components/compounds/Chord.vue` is authoritative, and the guide mounts the real source; its coupled display anatomy, paper-cut material, geometry, type scale, and Ink-to-color articulation are closed | Acceptance follows verified commit `6ddc3fb`. No production consumer has adopted Chord yet. Return to CodeStrip duration, Rest, density, and temporal states before production adoption or the later action compound |
| 2026-09-01 | Complete CodeStrip workbench | Expanded the authoritative CodeStrip candidate and real guide specimen to compose accepted Note and Chord, replace bracket-chord and bare-rest stand-ins with first-class Chord and surfaced Rest tokens, expose inline/stacked/distance/bar/hidden duration modes, compare dense/default/spaced rows, and drive controlled Ink-to-color/Ivory temporal fills from a specimen-only clock | 22/22 focused CodeStrip + Chord + Note tests, type-check, production build, and diff check pass with existing dependency/chunk warnings. Live computed styles found the hero contained with correct real-source anatomy, Ink progress covers, unchanged Chord colors, surfaced Rest, every duration mode, and no desktop specimen overflow. Screenshot capture and narrow resize failed and are not claimed. No production adoption; inspect `#unique-code-strip` and select duration/density next |
| 2026-09-01 | CodeStrip duration refinement | Burooj retired inline-text and proportional-distance duration. The remaining public modes are stacked text, bar marks, and hidden; bar mode now derives repeated subdivision marks from `timeSignature`, emphasizes beat starts, and includes Rest without printing a Rest duration. Chord member order is documented as caller-supplied presentation/voicing order with independent progress, not onset order or chord detection | 24/24 focused CodeStrip + Chord + Note tests, type-check, production build, and diff check pass with existing dependency/chunk warnings. Live computed styles mounted no retired modes; equal events produced 15 marks in 4/4 and 12 in 3/4, including the Rest mark, with all duration rows contained. Production chord-order/detection semantics are parked outside this visual slice; no production adoption yet |
| 2026-09-01 | CodeStrip split-bar correction | Returned bar duration to the old horizontal proportional-space idea: each Note, Chord, and Rest now keeps its duration inline as one split meter bar beside the event instead of stacking a second duration row. The guide now labels that treatment as split proportional meter bars, and its chord examples seed voicing-order versus press-order metadata without moving chord-detection logic into CodeStrip | Focused `CodeStripNote` coverage passes 5/5 after asserting inline proportional bars for Note and Rest; type-check, production build, and `git diff --check` pass with the existing Browserslist staleness, `@strudel/soundfonts` eval, and bundle-size warnings. No fresh live browser capture or narrow resize claim in this receipt. CodeStrip remains under review and not production-adopted |
| 2026-09-01 | Chord ordering formalization | Chord members now carry separate optional `voicingOrder` and `pressOrder` metadata. Fused symbol bands sort low-to-high by voicing while clustered Notes sort by press chronology; each member's attack/release progress remains attached to that member through either presentation, with stable input order as the fallback | 24/24 focused CodeStrip + Chord + Note tests, type-check, production build, and diff check pass. Live guide inspection confirmed the clustered `E4 B4 C4 G4` press order, distinct fused/clustered accessible names, split proportional bars for Note/Chord/Rest, 4/4 versus 3/4 mark counts, and no section overflow. A dedicated FloatingPopup inversion bug could not be created because Linear returned `USAGE_LIMIT_EXCEEDED`; the exact deferred bug brief is preserved in a Cockpit-authored comment on `BJS-35` pending issue capacity |
| 2026-09-02 | CodeStrip visual acceptance | Burooj accepted the complete corrected CodeStrip workbench after inspecting it live. The accepted unique composes Note and Chord, owns surfaced Rest, keeps stacked text / split proportional meter bars / hidden duration treatments and dense / default / spaced host densities, and preserves independent Ink-to-color or Ink-to-Ivory temporal progress | `components/uniques/CodeStrip.vue` is authoritative and `style-guide/uniques/UniqueCodeStrip.vue` is its real specimen. Prior verification at `1494070` and `48bab6d` remains the implementation evidence. Production `LiveStrip.vue` adoption and fresh production/lineage verification remain open; `BJS-406` separately owns FloatingPopup inversion detection |
| 2026-09-02 | CodeStrip production adoption | Replaced LiveStrip's duplicated non-editor token markup and CSS with the authoritative CodeStrip while preserving its editable Strudel branch and runtime responsibilities. A local semantic adapter maps recorded timing into Note, clustered-overlap Chord, Rest, duration, separate voicing/press order, and independent playback progress; it owns no visual grammar or recognition. CodeStrip owns the optional horizontal scroll surface and production follow targets it | `dd2ff7f`; 82/82 focused tests, type-check, production build, and diff check pass with existing dependency/chunk warnings. Mounted LiveStrip coverage proves both config branches, generated-code/editor attachment, onDraw progress, smooth follow-scroll, stop reset, and teardown. Live production inspection confirmed one real CodeStrip at the host, clustered `G4 C4 E4` overlap plus Rest/Note ordering, stacked duration policy, a 20-token 320px scroll surface (`635px` content in `277px` viewport), and no document overflow. The guide retained 18 real-source specimens with all three duration modes and no overflow. Screenshot capture failed and is not claimed. Fresh bounded lineage audit passed after its initial test-gap finding was repaired; the action cluster is next |
| 2026-09-02 | CodeStrip adoption correction | Burooj's phone review exposed that `dd2ff7f` had misunderstood the successor boundary: it created a separate CodeStrip renderer while preserving the old independently styled Strudel mirror behind `showStrudelLine`. Corrected production to one Strudel CodeMirror whose blurred replacement widget mounts authoritative `CodeStrip.vue`; focus reveals that same mirror's raw editable source. Removed the parallel supplement, legacy token widgets/CSS, and obsolete display fork. Strudel `onDraw` phase now directly controls the mounted CodeStrip's progressive fill and active follow target | `11c5736`, with mirror regression coverage hardened at `6c1bf85`; 81/81 focused tests, type-check, production build, and diff check pass with the existing Browserslist, soundfont-eval, and bundle-size warnings. Live production inspection found one `.live-strip`, one `.cm-code-strip-widget`, one real `.code-strip`, and zero legacy `.cm-live-strip-token` nodes. A forced 390px host stayed document-contained; six events fit and a longer 12-event sequence scrolled internally. Playback samples advanced first/next Note progress from `0.385 -> 1` and `0 -> 0.150 -> 0.763`; focus removed the widget and exposed raw Strudel, while blur restored CodeStrip. A desktop screenshot was captured after earlier snapshot failures. A fresh bounded lineage re-audit passed after both minor regression-coverage findings were repaired |
| 2026-09-02 | CodeStrip document-boundary correction | Burooj clarified that CodeStrip is not a visual mounted over or beside Strudel: it is the styled Strudel CodeMirror document, and progressive fill is Strudel's own source-location highlight. Replaced the whole-document Vue widget and independent `onDraw` phase with per-event CodeMirror replacements driven by exported `showMiniLocations`; idle events are fully colored, Play immediately resets them to Ink, native note/chord source ranges reveal their fills, and Rest advances from the same native `atTime`. Removed LiveStrip, LiveCard, all LiveStrip config/controller/runtime names, and folded the production shell into the sole public `CodeStrip/index.vue`; legacy persisted config migrates to `codeStrip` | `4b3e49a`, with source-edit authority hardened after audit at `a88c583`; 76/76 focused tests, type-check, production build, and diff check pass with existing Browserslist, soundfont-eval, and bundle-size warnings. Live production DOM inspection found one `.code-strip`, one CodeMirror, six per-source `.cm-code-strip-event` replacements, zero `.live-strip`/`.live-card` nodes, and zero whole-document widgets. Idle-to-Play sampled `1 -> 0` synchronously. Focus-in exposed the unchanged raw mini-notation and removed only event replacements; blur restored them. A forced 390px host stayed internally scrollable. The guide mounts 18 real CodeMirror specimens and routes controlled progress through the same native effect. The fresh bounded audit found one stale-semantic edit gap; the repair now proves `C4` to `D4` re-derives Re and follows the edited native highlight range. Collaborative screenshot remained unavailable and is not claimed. Full suite remains baseline-red outside this unit: 41 files / 377 tests pass and 14 files / 13 tests fail on existing GSAP mocks, stale imports, environment assumptions, and unrelated utility expectations |
| 2026-09-03 | CodeStrip action compound definition | Burooj accepted one compact row with brass Play/Stop left of the flexible CodeStrip and ink Backspace plus ink Return on the right. Backspace truthfully names remove-last-event rather than editor undo; Return carries the typewriter carriage-return metaphor for commit-and-clear rather than Send. All three are icon-only 40px Buttons without visible labels | `5625b45`; formalize one compound source and real specimen, then replace the production `KeyboardActionBar.vue` presentation without changing behavior |
| 2026-09-04 | CodeStrip action compound formalization and adoption | Created authoritative `CodeStripActions.vue`, composing real Button and CodeStrip sources; mounted its real controlled specimen; adopted it through `DrawerKeyboard.vue`; and removed the mixed legacy action bar. The audit then restored CodeMirror text selection and preserved all six displaced settings through an explicit production-only `LegacyKeyboardControls.vue` sibling pending their later destination units | `7b7c7b6`, `6867a42`; 27/27 focused tests, type-check/build, live guide screenshot/DOM, 390px production screenshot/geometry before the nonvisual audit repair, and fresh bounded lineage re-audit pass. Existing Browserslist, soundfont-eval, and bundle-size warnings remain |
| 2026-09-04 | CodeStrip action correction and Control Bar definition | Burooj removed the action compound's outer padding, reassigned Return to an Ivory face with Ink icon, and accepted Control Bar as the name and equal-slot mobile-first layout for the current six Knobs. The proposed Drawer relocation is explicitly deferred; its Boolean control remains unchanged in Control Bar until Drawer is defined | Formalize the accepted correction and promote `LegacyKeyboardControls.vue` into a controlled real compound plus guide specimen; preserve all existing actions and store ownership |
| 2026-09-04 | Control Bar formalization and action correction | Created controlled authoritative `ControlBar.vue`, mounted its real specimen, adopted it in `DrawerKeyboard.vue`, and retired the production-only legacy sibling. Six equal-width Knob slots replace the max-content scroller; CodeStrip Actions now has no outer padding and Return is Ivory/Ink. Drawer remains the same provisional Boolean Knob pending its own definition | `f4d4225`; 51/51 focused tests, type-check/build, both local routes HTTP 200, and fresh bounded lineage audit pass. Existing Browserslist, soundfont-eval, and bundle-size warnings remain. Collaborative preview navigation failed, so no fresh visual capture is claimed |
| 2026-09-04 | CodeStrip action density correction | Burooj's deployed inspection showed that removing the compound wrapper padding did not remove the visible waste: the remaining inset belongs to CodeMirror content and the default-density event row. The accepted mobile-first correction makes CodeStrip Actions request dense presentation and reduces dense content padding to 2px block / 4px inline, without changing CodeStrip's other density contexts | Adopt through the existing CodeStrip density seam; preserve editing, event anatomy, duration, selection, scrolling, and action behavior |
| 2026-09-04 | CodeStrip action density adoption | Defaulted only CodeStrip Actions to the dense CodeStrip treatment and tightened CodeStrip-owned dense content inset from the inherited 7px/10px box to 2px block / 4px inline. Public default/spaced densities and PatternCard remain unchanged | `6226d6f`; 20/20 directly focused tests, type-check/build, both local routes HTTP 200, and fresh bounded lineage audit pass with 26 independent focused tests. Existing warnings remain; collaborative preview navigation failed, so no corrected capture is claimed |
| 2026-09-04 | Knob Options label and rip-mode correction | Replaced the clipping 58cqi ellipsis box with an 84cqi center viewport that preserves Lets Jazz overhang, compacts long labels, and wraps multi-word modes. Options value changes now pair the old tear-up and new slide-in phases recovered from the v2 Mode Knob through global rip-mode duration/easing/keyframes. Analog and Digital specimens use real `MODE_OPTIONS`, and the Motion token specimen consumes the production cadence with a stable Reduced Motion state | `699cfd7`, `6cb9f18`; 16/16 focused Knob + Control Bar tests, type-check, production build, compiled-CSS token check, both local routes HTTP 200, and fresh bounded lineage re-audit pass. Production and guide retain the same public Knob seam; no gesture, haptic, value, or ring-motion behavior changed. Collaborative preview navigation failed, so narrow rendered fit at 42–47px remains an explicit visual-verification risk and no screenshot is claimed |
| 2026-09-04 | CodeStrip flush correction | Burooj's second deployed inspection rejected the remaining 2px/4px compromise. Dense CodeStrip content in the action compound is accepted fully flush with zero inset; other CodeStrip density contexts remain unchanged | Implement at the existing dense owner and reverify action composition, editing, and lineage |
| 2026-09-04 | CodeStrip flush adoption | Removed the dense CodeMirror content inset completely while keeping CodeStrip's public default and spaced contexts unchanged | `f33fef9`; 20/20 directly focused tests, type-check/build, and fresh bounded lineage audit pass with 26 independent focused tests. No corrected capture claimed before Vercel inspection |
| 2026-09-04 | CodeStrip Bar definition | Burooj rejected the remaining framed-object composition: despite zero content inset, the outlined CodeStrip and exposed gaps made the Buttons feel separate. Accepted the name CodeStrip Bar and one continuous Ink-2 rail with no outline or inter-item gaps; CodeStrip becomes unframed and transparent only in this compound | Rename the authoritative source/specimen/tests, expose the frame treatment at CodeStrip's own seam, and preserve all behavior plus other CodeStrip consumers |
| 2026-09-04 | CodeStrip Bar adoption | Renamed the authoritative compound, real specimen, production import, and focused test; placed the three Buttons and unframed transparent dense CodeStrip on one continuous Ink-2 rail with no exposed gaps. CodeStrip's new `framed` seam defaults on everywhere else | `dd08bb6`; 52/52 focused tests, type-check/build, both local routes HTTP 200, and fresh bounded lineage audit pass after repairing three stale current-truth names. Existing warnings remain; collaborative preview navigation failed, so no corrected capture is claimed before Vercel inspection |
| 2026-09-04 | CodeStrip Bar surface correction | Burooj's deployed inspection rejected the opaque Ink-2 rail as inconsistent with Control Bar. The accepted correction uses one shared translucent black instrument-bar surface and backdrop recipe for both compounds, while CodeStrip Bar remains borderless, gapless, and flush | Promote the reusable recipe to shared tokens, make both compounds consume it, and preserve Control Bar's existing bottom divider plus all component behavior |
| 2026-09-04 | CodeStrip Bar surface adoption | Promoted Control Bar's translucent black surface and backdrop treatment into shared instrument-bar tokens, then made CodeStrip Bar and Control Bar consume the same plane. CodeStrip Bar remains borderless, gapless, and flush; Control Bar keeps its own bottom divider | `5516c05`; 50/50 focused tests, type-check/build, both local routes HTTP 200, and fresh bounded lineage audit pass with 40 independent focused tests. Existing warnings remain. Collaborative preview navigation failed, so no corrected capture is claimed before Vercel inspection |
| 2026-09-04 | CodeStrip Bar editor-plane correction | Burooj's mobile inspection exposed Strudel's redundant root background and active-line paint as the remaining inner rectangle; the apparent gap was broken surface continuity rather than grid spacing. CodeStrip now owns a genuinely transparent unframed editor, vertically aligns one-line content to the 40px rail, and gives the empty document a short centered recording prompt. The real compound specimen includes that state; framed consumers and all behavior remain unchanged | `5208e49`; adjacent 49-test matrix and final 13 directly focused tests pass, as do type-check, production build, both route checks, and a fresh bounded lineage re-audit after closing two regression-coverage findings. Existing build warnings remain. T3 preview navigation failed, so the supplied screenshots are rejection evidence and no corrected visual capture is claimed |

| 2026-09-06 | Drawer definition accepted | Burooj accepted one full-width top/bottom unique with clean aligned icon/grip handles, continuous drag height and progressive bottom-stack collapse, tap-to-hide-keys/restore behavior, independent persistent preferred heights, mutually exclusive top panels, interactive exposed canvas, and trigger replacement. Instrument name/MIDI status remain; MIDI shortcut moves inside Config; Drawer sizing replaces Key Size. Keyboard row count and density gate remain independent | Definition only. Implement shared source, three production consumers, and real guide; preserve musical/editing/MIDI contracts and verify expressly authorized UX changes |

| 2026-09-06 | Drawer formalization, adoption, and verification | One authoritative `uniques/Drawer/index.vue` now supplies production Keyboard and top Instrument/Config plus the real guide. Continuous height drives Keyboard-owned proportions/minimums, progressively clips the complete bottom stack, and persists independent preferred heights. Clean aligned handles replace the old triggers and Drawer Knob; Key Size is retired. Top panels are exclusive, scroll internally, leave exposed canvas interactive, and preserve Instrument identity/MIDI status. The bounded audit found and the repair closed focus leakage from partially clipped controls and keys | `a4fb2a6` source/production, `62653b2` specimens, `a2edb20` / `8216e7b` focus repairs. 85 focused/adjacent tests pass, with final 10 Drawer/TopDrawer tests after repair; type-check/build and fresh bounded lineage re-audit pass. Chromium production 320/390/768/960px and guide 320/390/768px verify full-width geometry, native drag/tap/Enter/Space, collapse/restore/reload, top scrolling/switching/Escape, Reduced Motion, forced colors, and blocked focus on clipped keys. Evidence: `evidence/drawer-2026-09-06/`. Existing build warnings remain; no real touch hardware or audio-output claim. No push; pre-existing `notes/` untouched. Next: Keyboard density, then DrawerKeyboard composition |

| 2026-09-06 | Drawer phone-review refinement accepted | Burooj liked the feel, requested smaller/tighter handles and the original playful bounce, top-panel dismissal when touching stage or keyboard, and top handles reopening to full content rather than remembered height. Full content remains capped to the viewport; keyboard height memory stays intact | Implement the explicit correction and exercise it through computer/browser interaction, then verify and audit |

| 2026-09-06 | Drawer phone-review refinement adopted | Tightened shared handles to 28px height and 4px/6px padding (keyboard 46px wide); restored existing 360ms Swing tokens with content retained through closing. Top panels now dismiss on outside pointer/touch without intercepting stage/keyboard input and reopen to measured full content capped to the viewport; bottom height memory remains unchanged. Natural body/chrome measurement belongs to OverlayPanelShell and is forwarded to the shared Drawer | `d10cfc0` acceptance; `d6c5c60` implementation. 35 focused/adjacent tests, type-check/build, and fresh bounded lineage audit pass. Local Chromium 320/390/960px verified reduced-drag reopen, outside input pass-through, 6.625px Swing overshoot/settle, closing-content retention, and Reduced Motion cleanup; independent audit checked all16 Config tabs at960px. Evidence: `evidence/drawer-2026-09-06-feedback/`. Baseline `cf55997` independently reproduces the keyboard-triggered CodeMirror reentrancy/TilePointer bug, parked outside Drawer. Existing build warnings remain; collaborative preview navigation failed, local Chromium interaction/captures succeeded. Authorized preview-branch push follows; pre-existing `notes/` untouched. Next visual unit remains Keyboard density |

| 2026-09-06 | Drawer handle screenshot correction accepted | Burooj requested unclipped Instrument text, a centered MIDI dot with whole-gear glow, and compact horizontal grips flanking the icon/identity, following the supplied reference | Explicit visual correction; preserve Drawer behavior and verify production plus real guide |

| 2026-09-06 | Drawer compact grips and status gear adopted | Shared Drawer now flanks its identity with two 8px horizontal grips, uses 4px padding, and gives Jazz labels a 20px line box inside the unchanged 28px tab. Shared MidiSettingsIcon centers the status dot in the gear and glows across both; Config and the real guide consume it. Config and Keyboard handles are 46px wide. Native MIDI status tooltip retained after audit | `65e860e` acceptance, `7e1f40d` source/specimen, `c59f4e2` tooltip repair. 22 focused tests, type-check/build, diff check, and fresh bounded lineage re-audit pass. Chromium production 320/390/960px and guide 320px verify taps/Escape/drag, label descenders and long-name ellipsis, all MIDI states with exact dot centering, Reduced Motion pulse suppression, and forced-colors contrast. No browser page errors. Screenshots and measurements: `evidence/drawer-2026-09-06-grips/`. Existing build warnings and separately parked CodeMirror bug remain; no real touch hardware/MIDI device claim. Authorized preview push follows; pre-existing notes/ untouched. Next unit remains Keyboard density |

| 2026-09-06 | Drawer handle alignment and identity correction | Burooj identified the label still sitting below the centered grip/icon and questioned waveform as an instrument affordance. Correct the optical baseline within the unclipped label box and replace AudioLines with Piano in production and guide | Small visual correction; preserve interactions and current instrument name |

| 2026-09-06 | Drawer optical alignment and instrument identity adopted | Raised the shared Jazz handle label 2px while retaining its 20px unclipped line box. Production InstrumentSelector and real Drawer guide now use the same Piano icon through the existing icon slot | 16 focused tests, type-check via production build, diff check, and fresh bounded lineage audit pass. Chromium verified unchanged 28px handles at production 320/390/960px, open/Escape, and 320px guide captures for Piano, Bagpipe, Electric Grand Piano. Evidence: `evidence/drawer-2026-09-06-alignment/`. Existing build warnings remain. Authorized preview push follows; notes/ untouched. Next unit remains Keyboard density |

| 2026-09-06 | Selected-instrument handle identity accepted | Burooj rejected the fixed piano icon for other selections and instructed implementation of selected-instrument icons with name-only fallback where no suitable icon exists | Preserve optical alignment, name, selection behavior, and shared production/guide ownership |

| 2026-09-06 | Selected-instrument icon and fallback adopted | One presentation resolver supplies matching Piano/Guitar/Drum icons for known sound names; unsupported sounds retain only their name between grips. Production and real guide use the same resolver, and TopDrawer/Drawer omit the absent icon slot without a spacer. Guide choices now drive its controlled identity | 19 focused tests including reactive selection/fallback regression, type-check via build, diff check, and fresh bounded lineage audit pass. Chromium production 390px exercises piano/guitar/drum/violin/sine/piano selection; 320px capture verifies compact fallback; real guide exercises all three icons plus violin/flute/synth/custom fallbacks. No page errors. Evidence: `evidence/drawer-2026-09-06-instrument-icons/`. Existing build warnings remain; sound output/real MIDI hardware not tested. Authorized preview push follows; notes/ untouched. Next unit remains Keyboard density |

| 2026-09-06 | CodeStrip Bar edge-spacing correction | Burooj’s screenshots reject edge-hugging actions and clipped Play. The shared compound uses existing spacing tokens for 8px block / 12px inline inset, 8px beside CodeStrip, and 6px between Backspace and Return. Continuous surface, dense unframed editor, 40px Button identities, and action behavior remain. Exact spacing is an implementation choice for review; no new primitive or token | 16 focused tests, type-check/build, and diff check pass. Chromium production at 320/390/550/960px verifies empty and populated semantic strips, contained buttons, internal scrolling, and no page overflow. Guide source geometry/actions pass at 320/390/960px viewports; gallery scaffold is not phone-responsive. No page errors; audio/touch hardware not tested. Evidence: `evidence/codestrip-bar-2026-09-06/`. Fresh bounded lineage audit passes with no findings. Branch `t3code/improve-codestrip-bar`; implementation `badb4dd`, not pushed. Next unit remains Keyboard density |

| 2026-09-06 | CodeStrip Bar buttons reduced 20% | At Burooj’s explicit request, all three actions consume Button’s existing `sm` size: 32px instead of 40px. Icons scale proportionally; inset, gaps, material, editor, and behavior remain unchanged. Production and guide share the authoritative compound | 8 focused tests, type-check/build, diff check, and bounded lineage audit pass. Chromium empty/populated production 320/390/550/960px and real guide host checks at 320/390/960px viewports verify 32px faces, containment, scrolling, and actions; no page errors. Evidence: `evidence/codestrip-bar-32px-2026-09-06/`. Branch `t3code/improve-codestrip-bar`, not pushed. Next unit remains Keyboard density |

| 2026-09-06 | Merged-stack review follow-up | Functional corrections for PRs #27–#29 preserve measured rests, source-owned CodeStrip pitch/duration, safe presentation dispatches, keyboard key release and padding, usable Drawer restore, settings access/state, and Knob input feedback. Retired ineffective keyboard glass/shape controls with saved-config migration; guide typography and specimens no longer enter the production route globally | 219 tests across 28 suites, type-check/build, diff check, and bounded audit pass. Chromium reproduces and clears the recording error and verifies settings hit targets at 320px, touch → keyboard input, rapid labels, and route isolation. `docs/reviews/stack-27-29.md` maps every review disposition and evidence. Loading Screen remains provisional and its review thread awaits Burooj’s decision; no new visual acceptance is inferred |

| 2026-09-06 | Review follow-up completed | Restored the previous production LoadingSplash after confirming prototype copy had shipped; the new LoadingScreen remains in the guide pending acceptance. Follow-up review corrections preserve flat-root and sparse-scale note spelling and expose Keyboard Padding in the controlled guide seam | 224 tests across 29 suites, type-check/build and diff check pass. Chromium verifies entry through restored splash and three recorded notes without page errors. This supersedes the preceding pending-decision status; no new visual acceptance is inferred |
| 2026-09-07 | Playable chord row and Joystick direction accepted | Burooj accepted a permanent number-row-like playable chord row whose cardinality follows the actual scale, plus one distinct Harmony Joystick instead of a second Harmony Knob. Center is automatic; tap latches; hold momentarily overrides and restores; explicit HiChord-inspired directions may borrow pitches. Exact audio and independent input ownership are part of the feature, while broader Keyboard density remains under review | Implement one pure harmony owner, one exact-pitch seam, shared Key-like multi-contact input, real Joystick and Keyboard specimens, production Control Bar/Drawer wiring, and focused lifecycle/accessibility coverage. Official direction reference: `https://manual.hichord.shop/` |
| 2026-09-07 | Playable chord row and Joystick adopted | `domain/harmony.ts` generates one chord per 5/6/7/12-degree scale with explicit seven-note, sparse, chromatic, alteration, and voicing policies. Keyboard composes fused accepted Chords through `ChordKey`, snapshots alteration at attack, and routes melody/chord owners through independent resolved/pending voice groups and exact pitches. Control Bar now holds five Knobs plus one distinct Joystick; both authoritative sources have real controlled specimens. Browser review found and repaired the Joystick's collapsing hero width and clipped Harmony label. The bounded audit then repaired canceled-pointer keyboard activation, removed the stale competing input lifecycle, and preserved borrowed-pitch chromatic identity through recording and CodeStrip. Keyboard's broader density gate remains open | Commits `7f34194`, `2e8d1a9`, `72f2230`, `8b6c194`, `cc3d5d8`, `fce6b2b`, `7944086`, `a852d19`, `a8b9b3c`. The final 132-test focused matrix, type-check, build, and diff check pass; a fresh bounded lineage re-audit independently passed 16 suites / 110 tests with no remaining findings. Full suite: 52 files / 484 tests pass; unrelated baseline remains 11 files / 13 tests. Chrome 147 production 390/960px and guide 320px Keyboard/960px Joystick verify cardinality, deliberate chromatic overflow, no production overflow, latch/hold restore, Reduced Motion, forced colors, and no page errors. Evidence: `evidence/chord-row-joystick-2026-09-07/`. No real touch hardware or speaker-output claim |
| 2026-09-07 | Chord color and Joystick correction accepted | Burooj rejected black resting chord faces, edge-tap interaction, and Primitive taxonomy. Chord keys expose the fused low-to-high colors of their actual Note members at rest. Joystick is one brass Unique with shared Analog and Digital treatments; it borrows Knob's relative-drag principle without becoming a Knob. Press alone is inert, short drag latches, and held drag momentarily overrides and restores. The Joystick remains the sole visible Harmony control because automatic harmonization is policy, not another user axis; a separate Knob is deferred until an independent harmony-bank choice exists | Correct the production source and real guide, preserve center plus eight HiChord-inspired directions and keyboard access, and verify the rejected edge-press case explicitly |
| 2026-09-07 | Chord color and Joystick correction adopted | Keyboard supplies full resting progress only to its fused Chord consumer, preserving CodeStrip playback progress. `components/uniques/Joystick/index.vue` replaces the retired Primitive and owns brass Analog/Digital rendering plus one full-face two-axis gesture. Pointer-down preserves the current vector; movement beyond 5px adds press-relative displacement, short release latches, and a held drag restores the prior latch. Production Control Bar explicitly chooses Analog; the real Unique specimen drives both treatments | `3b972f0`, `a6f2d0b`. Eight focused suites / 50 tests, type-check, production build, and diff check pass with existing build warnings. A fresh bounded lineage audit found no findings, reproduced the exact 50/50 receipt, and passed a broader 8-suite / 56-test consumer substitution. T3 Code Chrome 150 at measured 1402×877 verifies Analog production, Analog/Digital real specimens, edge-press no-op, relative latch/center/held-restore transitions, C ↔ Cmaj7 chord mutation, and full member hues at rest. Resize failed, so no fresh narrow or media-emulation capture is claimed. Evidence: `evidence/joystick-relative-drag-2026-09-07/`. Physical touch and audio hardware remain untested; Keyboard's broader density gate stays open |
| 2026-09-07 | Joystick page edition and chord-row review follow-up | Joystick now applies Knob PR #43's edition principle independently: all implicit production instances share one page-stable Analog/Digital treatment that alternates by app load, while the guide route does not advance storage and every guide specimen is explicit. Seven PR review findings are repaired: chord-row touch pan, Drawer height reservation, held-voicing rendering, focus-exit release, exact borrowed MIDI/visual identity, and key-relative octave coordinates. Audit repairs preserve the selected edition through blocked writes and keep a held degree visible/releasable across cardinality changes | `a4664af`, `3a1bc73`, `381e6c8`. Twelve focused suites / 72 tests, type-check, production build, and diff check pass. The full suite reaches 55 passing files / 503 passing tests and retains 11 files / 13 tests of unrelated baseline failures across removed imports, GSAP mocks, and utility/environment expectations. Both local routes return HTTP 200. T3 collaborative preview rejected the worktree environment-port and its direct-localhost proxy returned 404, so no fresh screenshot, touch-hardware, MIDI-hardware, or speaker-output claim is made. Keyboard's broader density gate stays open |

| 2026-09-07 | Plan and taxonomy reset | Burooj established Unique as an orthogonal singular-artifact classification rather than a complexity rung; accepted PerformanceDeck as the bottom-drawer composition name; defined Stage as the unified canvas and UIBeat as a system-wide music/motion protocol; confirmed variable Keyboard cardinality including chromatic; and placed Pattern Card/List, top-menu interiors, brand loading artifacts, and Sticker/Badge adoption in the remaining work | Reworked `DESIGN_SYSTEM_TRACKER.md` into the sole executable Plan and kept this Log chronological, with no third ledger. Added dependency-aware parallel lanes, shared-file ownership, token/orphan dispositions, UIBeat runtime/config retirement gates, and a final runtime-surface lineage scan. Documentation-only; independently reviewed by Astra and `git diff --check` passes |

| 2026-09-07 | Knob drag-value Sticker accepted and adopted | Burooj accepted the filled Ivory Sticker above the drag contact, then accepted the livelier spring revision. Interactive Range and Options Knobs now expose immediate formatted values with contact-following lean, rebound, viewport containment/top-edge fallback, direct Reduced Motion tracking, and complete gesture cleanup. Sticker owns the paper geometry/material; Knob owns visibility, content, motion, placement, and input lifecycle. The existing real Analog/Digital specimens and production Control Bar consume the same public Knob seam | `0f1e4dc` initial follower, `79a2727` spring motion, `813b81a` touch/inline-viewport repair, `34212c9` wrapped-height repair. 24/24 focused Knob + Control Bar tests, type-check/build, diff check, and fresh bounded lineage audit pass. Chromium verifies production and guide behavior, 5× pinch-zoom containment in both axes with complete “Major Pentatonic” text, Reduced Motion, edge placement, and simultaneous keyboard/Knob touch isolation. Existing build warnings remain; no physical multi-touch hardware claim |

| 2026-09-07 | Brass Knob follower uses Badge | Burooj assigned the accepted Badge identity to the drag follower whenever a Knob is Brass. The public Knob source maps Brass to Sticker's brass-sheen Badge variant and Ivory to its existing filled paper variant; Badge retains its mono type, edge, uppercase identity, and sheen while Knob retains placement and spring behavior. Both real Analog/Digital specimens exercise Brass; current production compositions remain Ivory, so no Brass production-instance adoption is claimed and Badge taxonomy remains open | `e681f6f`; 25/25 focused Knob + Control Bar tests, type-check/build, diff check, and fresh bounded lineage audit pass. Chromium at 390px confirms brass Badge anatomy and 12px mono type in the real Analog guide, filled 20px Ivory treatment in the same source specimen, and unchanged current production Ivory. Existing build warnings remain |

| 2026-09-07 | Knob alternating page edition | At Burooj's explicit request, production Knobs that do not pin a visual now share one presentation per app load and alternate Analog Ring → Digital Arc → Analog Ring across consecutive loads. The old Arc default makes a user's first new edition Ring. The private edition owner persists only the last production choice; explicit Analog/Digital specimens stay fixed and the guide route does not advance the sequence | `de55863`; 25/25 Knob, edition, Control Bar, and app-entry tests pass, as do type-check, production build, and diff check. Local Chrome proves all five Control Bar Knobs render Ring on load one and Arc after refresh, with the matching persisted value; the guide retains 7/7 Ring and 7/7 Arc specimens without changing storage. Existing Browserslist, soundfont-eval, mixed-import, and bundle-size warnings remain. Collaborative preview routing failed, so this is DOM/live-interaction evidence rather than a collaborative screenshot. Branch `t3code/alternate-knob-presentation` was not pushed at receipt time; Keyboard density remains the next visual unit |
| 2026-09-07 | Joystick finish, alignment, floating label, and borrowed-color correction | Burooj's deployed screenshots accepted the Joystick direction but requested canonical brass sheen, exact Knob alignment/label anatomy, the PR #37 floating label, ring-centered detents, and no blank chord bands. One shared primitive control frame now supplies Knob/Joystick size, centering, and label geometry; promoted `DragValue.vue` supplies their common spring/viewport follower while each control retains gesture ownership. Joystick applies the canonical animated sheen to its brass hardware and exposes direction names as Brass Badges. The exact-pitch visual resolver gives borrowed tones chromatic color without changing general movable or ROLI off-scale semantics | `db7fdb0`; 18 suites / 127 tests pass, as do type-check, production build, and diff check. Full suite: 56 files / 517 tests pass; the remaining 11 files / 12 tests are the existing removed-import, GSAP-mock, and utility/environment baseline. T3 production DOM at 1402×877 resolves all six faces to 72×72 on one baseline and every label to 12px with a 2.16px gap; the sheen animation and uniform 31.68px detent radius are active. A synthetic real-source drag shows and removes the shared Jazzy Brass Badge, and all nine harmony states report zero empty/neutral chord bands. Snapshot and resize calls failed, so no fresh screenshot or narrow-width claim is made; physical touch, MIDI hardware, and speaker output remain untested. Keyboard density remains the next visual frontier |
| 2026-09-07 | Chord-row late review and mainline integration | Three late PR findings were checked after the finish correction. Focus-held chords now retain their attacked snapshot and voices through a cardinality remap until keyup; click-only native-button activation supplies one 120ms independently owned chord pulse without replaying pointer/keyboard cycles. Tonal already returned `null` for out-of-range C#10, but the mirrored-MIDI boundary now explicitly accepts only integer notes 0–127. Current `main`'s Humming transport was merged alongside Harmony in the shared Drawer host, and its concise executable design plan remains authoritative | `3623542`, merge `8fd9f5f`; merged focused matrix 27 suites / 189 tests, type-check, production build, and diff checks pass. Full suite: 62 files / 558 tests pass; the unchanged 11 files / 13 tests remain in removed-import, GSAP-mock, and utility/environment baseline categories. No additional browser or hardware claim was made during this functional review follow-up; the preceding Joystick DOM measurements and nine-direction color inspection remain the current visual evidence. Keyboard density remains the next visual frontier |
| 2026-09-07 | Exact standalone Note color and Joystick radio semantics | Late review found two remaining consumer gaps. `Note.vue` now treats an explicit `pitchClassIndex` as authoritative and uses the exact visual resolver, so standalone borrowed CodeStrip notes retain the same chromatic identity as fused/clustered Chords. Joystick arrow/Home navigation now selects its destination and immediately transfers the roving tab stop instead of moving focus alone; the controlled value update then moves `aria-checked` and the visible state | `997cca7`; final merged feature matrix 32 suites / 244 tests, type-check, production build, and diff check pass. The preceding full-suite result remains 62 files / 558 tests with the unchanged 11-file / 13-test unrelated baseline. No new visual capture or hardware claim; existing Joystick alignment/sheen and nine-direction color evidence remains current |
| 2026-09-07 | Chord-row final review corrections | Held chord faces now remain visual snapshots while new owners attack current Harmony. Exact pitch identity reaches Pattern Card and particle colors; scientific and keyboard-row octave coordinates are explicit end-to-end; extreme extended voicings shift by whole octaves into MIDI range; mirrored unisons use pitch reference counts so one owner cannot silence another | `34bc003`; 13 focused suites / 83 tests, production build, and diff check pass. Full suite: 64 files / 566 tests pass; the unchanged 11 files / 13 tests remain in removed-import, GSAP-mock, and utility/environment baseline categories. Fresh bounded lineage audit confirms the existing resolver remains the lowest exact-color owner and all changed production consumers retain their source ownership. No new visual or hardware claim |
| 2026-09-07 | Post-push chord context and string identity correction | A later automated review exposed two consumers missed by the first octave/context pass. Each held chord owner now snapshots key and mode alongside its attacked voicing, so the face retains the sounding chord's color context until release. String event activations now prefer keyboard-row octave while pitch/color consumers retain scientific octave, preventing one wrapped pitch from vibrating two visible strings | `b155671`; both exact regressions went red before repair, then 11 adjacent suites / 72 tests, type-check, production build, diff check, and bounded lineage re-audit passed. Existing build warnings remain. No new visual or hardware claim |
| 2026-09-07 | Chord-row touch pan intent correction | Completed post-push review found that an overflowing chromatic row could scroll but still attacked on touchstart. ChordKey now opts into a 120ms touch-intent window through the shared press lifecycle: 8px horizontal movement cancels silently, a quick stationary tap pulses once, and a stationary held thumb becomes a sustained independent owner. Melody Keys keep immediate touch response | `5b9d337`; the pan and hold regressions went red before repair, then 8 adjacent suites / 66 tests, type-check, production build, diff check, and bounded source/consumer audit passed. Existing build warnings remain. No physical touch-hardware claim |
| 2026-09-07 | Chord blend, Key depression, and Joystick optical parity | Burooj's production screenshot showed that an equal 72px layout box did not make the edge-painted Joystick optically equal to Knob's inset dial. The shared control support seam now owns both the 84% visible-hardware diameter and Knob's radial dark-well material; Joystick retains its full-face input target while painting brass at the same diameter as the Knob dome. Fused Chord members use boundary-matched pitch-color gradients while retaining independent progress, and every held chord owner snapshots its corresponding in-scale Key IDs so those Notes use the native Key down-state until release. Chord symbols remain Ivory because major/minor quality does not predict contrast across a multi-pitch gradient | `6c78a8d`; regressions failed at all three seams before repair. Nine adjacent suites / 95 tests, type-check, production build, and diff check pass with existing warnings. T3 production at the active phone preset measures equal 56.52px control faces, 47.48px Knob dome versus 47.47px Joystick hardware, identical label tops, the shared two-layer well, exact adjacent gradient boundary colors, and C/E/G entering the existing 2px/.97 down-state for one held C-chord owner before release. No physical touch, MIDI hardware, or speaker-output claim |
| 2026-09-07 | Chord-row final-head state hardening | Completed review tightened three cross-state seams without changing the accepted presentation: pending exact-pitch voices now reject stale instrument selections before entering app state; held chord depression follows the exact pitches still sounding rather than attack-time scale-degree coordinates; and exact-pitch chord gradients apply Keyboard brightness/saturation to the primary colors they actually compose | `bfe8de8`; 20 adjacent suites / 165 tests, type-check, production build, and diff check pass. The earlier full-suite baseline remains 610 passing tests with 13 unrelated failures plus six stale missing-import suites. No new browser or hardware claim |
| 2026-09-07 | Chord-row downstream pitch and event symmetry | The last active-note canvas consumers now route borrowed tones through exact pitch-class identity: Ambient keeps static cached gradients while Hilbert retains its animated color path. Pointer, touch, and click chord owners also retain the public press-time Harmony object through release, so external consumers see one symmetric lifecycle across live key, mode, or Harmony changes | `c6693b8`; 21 adjacent suites / 168 tests, type-check, production build, and diff check pass with existing warnings. No new browser or hardware claim |
| 2026-09-07 | Mark, Beat Indicator, and particles definition accepted | Burooj accepted one core Mark family containing structural and musical glyphs. Beat Indicator becomes a compound that selects Marks directly; Stage particles render the same authoritative Mark geometry with their existing Music Color and physics. Sparkle and mist retire. Kicker is not part of this lineage, and its source plus Spine Card remain untouched for their separate rework | Formalize one renderer-neutral geometry authority, rebuild the SVG Mark primitive and Beat compound around it, adopt it in Stage particles, and use the dedicated `/page` route for shared visual review. UIBeat timing/transport and the wider Stage definition remain outside this slice |
| 2026-09-07 | Unified Mark lineage formalized, adopted, and verified | One twenty-glyph registry now feeds the SVG Mark primitive and production canvas renderer. Beat Indicator moved to compounds and selects real Marks; Stage notes carry stable Mark identities while retaining Music Color and existing particle physics. Sparkle/mist and the orphaned global square-beat keyframes are retired. Real Mark and Beat specimens were rebuilt, and `/page` now shows the whole family, beat sets, note assignments, and actual particles. Kicker/Spine Card remain untouched; provisional Loading Screen triangle adoption and Beat production placement are explicitly deferred to their owning lanes | `3f2b797`, `add6cac`, `09ba6be`; 49/49 focused and adjacent tests, type-check, production build, diff check, and fresh Astra lineage audit pass. Tailscale collaborative preview at `100.88.219.36:5179/page` verified 20 Marks, compound sequences, live shared-path canvases, 488px no-overflow layout, and bounded preview particles. `/`, `/style-guide/`, and `/page` return 200. Existing Browserslist, soundfont-eval, mixed-import, bundle-size, Electron-sandbox, and duplicate-Strudel warnings remain; no UIBeat transport or wider Stage acceptance is claimed |
| 2026-09-08 | Mark drawing, particle selection, and Kicker definition corrected | Burooj reopened the glyph artwork for an Astra-low refinement and common-symbol expansion, rejected stable interval-to-Mark assignments in favor of randomized whole-family particles, and clarified that Kicker is not separate: its role is Mark inside Sticker. Spine Card remains owned by its separate rework | Redraw/extend the authoritative registry; remove Mark from note identity data; randomize each particle's Mark while preserving Music Color/physics; formalize Mark Sticker and retire the Kicker guide unit, leaving only a temporary compatibility adapter for current Spine Card |
| 2026-09-08 | Revised Mark candidate, Mark Sticker, and randomized particles implemented | Astra-low redrew the original family and added eight common musical glyphs, yielding one 28-Mark SVG/canvas registry. Production particles now choose independently from the complete registry and interval identity data carries no Mark. The former Kicker guide/source geometry is retired in favor of the real Mark Sticker compound; Spine Card itself is untouched and reaches the compound only through a temporary compatibility adapter. The dedicated `/page` workbench shows the full family, notation-heavy Beat sets, Mark Stickers, and the real Stage particle renderer | `8e0da8e`, `3935689`, `27f8d9f`, `6d81605`, `aac7fcd`; 49 focused/adjacent tests pass after the audit repair, as do type-check, production build, and diff check. Astra's read-only integration pass found no lineage defects and prompted the dot-only compatibility semantics repair. T3 shared preview at `http://100.88.219.36:5179/page` verifies 28 Mark tiles, four Mark Stickers, Beat and live particle surfaces with no page overflow; a scroll recording is `browser-recording-mtsb75h8`. Existing Browserslist, Strudel duplicate/eval, mixed-import, bundle-size, and Electron sandbox warnings remain. The revised glyph drawings remain a visual candidate pending Burooj's verdict; no acceptance is inferred |
| 2026-09-08 | Mark-in-Sticker promotion reversed | Burooj rejected Mark inside Sticker as a named compound: it is ordinary composition of two primitives, not a reusable design-system unit. The MarkSticker source and specimen are removed. The dedicated rough page demonstrates the recipe locally, while the temporary Kicker adapter composes Mark and Sticker directly for untouched Spine Card compatibility | This supersedes only the MarkSticker promotion in the preceding receipt. Mark, Beat Indicator, and randomized Stage particle work remain unchanged |
| 2026-09-07 | Harmonic Geometry review direction accepted | Burooj's deployed review found replay unreliable, all analysis labels too eager, and line edges too distinct from the blobs. The accepted revision makes labels optional and off by default, moves softened color connections beneath the blobs, and adds a distinct Merge presentation in which filled curved necks make blobs visually fuse, following the supplied metaball reference | Direction and preservation boundary are accepted; exact softness and fusion silhouette remain under review. Replay regressions, same-frame blob lifecycle coverage, and renderer behavior pass in a 72-test focused gate; type-check and production build pass; an Astra re-review found no remaining medium-or-higher correctness issue. PR #32 is the visual acceptance surface |
| 2026-09-07 | Harmonic Geometry overlay treatment rejected | Burooj's deployed inspection found the proposed Bézier neck and softened lines still read as something placed above or below the blobs, not as two blobs actually becoming one gooey body. The correction removes organic-mode bridge/graph painting and makes the actual prepared blob contours enter one shared material field; Outline must be that material's perimeter rather than an underlay | Supersedes only the first Outline/Merge visual candidate, not the optional-label or replay corrections. Exact replacement finish remains under review |
| 2026-09-07 | Harmonic Geometry shared-field candidate implemented | Blob preparation now owns one exact vibrating contour per body and shares it with ordinary and field rendering. A reusable, locally bounded Canvas 2D field rasterizes all bodies together, explicitly blurs/thresholds their union, opacity-weights color through release, and emits either a filled Merge silhouette or unified Outline perimeter. Production and the real guide consume the same seams; labels remain independently optional and off by default; Web/Center Only remain graph presentations | Focused field/geometry tests and type-check pass; real Chrome verifies opacity-weighted color withdrawal (`[128,0,128]` at equal weight to `[253,0,2]` at 1% blue), input-order independence, first-play/release/replay restoration, and a 29,756-pixel capped 4K/12-body field with approximately 12–14 ms warmed probes. Final Astra High audit reports no push blocker; bounded lineage audit reports no production duplicate. Mobile Safari performance and final visual acceptance remain open. PR #32 is the Vercel review surface |
| 2026-09-07 | Harmonic Geometry Merge character accepted; distance correction required | Burooj accepted the shared-field Merge appearance as looking great, then identified the remaining definition gap: separated blobs must still be one connected body at every distance, even if only a thin span remains between them | Preserve the accepted shared-material character and implement continuity inside that field rather than restoring a line or overlay. Exact long-range taper returns to the deployed review gate |
| 2026-09-07 | Harmonic Geometry always-connected Merge candidate implemented | Merge now adds one stable nearest-parent, contour-to-contour hourglass filament per additional body inside the authoritative material field. Natural close-range fusion remains; distance tapers the ribbon to a raster-aware continuity floor. Its gradient, endpoint opacity, blur, and threshold are processed with the blob bodies, so no separately painted bridge or complete graph returns. The guide uses farther fixture positions through the same production seam | `d81f935`; 73 focused tests, type-check, build, and diff check pass. Chrome verifies an 800px two-body separation with a continuous 24px waist versus 127px bodies, plus an opaque sampled diagonal across 3840×2160 at 29,670 field pixels and approximately 13–15ms warmed calls. Near/multi-body guide capture preserves the accepted material character. Fresh bounded lineage audit passes with no findings. Real-device Safari performance and Burooj's deployed acceptance of the long-range taper remain open; PR #32 is the review surface |
| 2026-09-07 | Harmonic Geometry long-range squareness correction | Burooj found the always-connected Merge close but still square in deployed use: fixed coarse ribbon segments, near-straight spans, and narrow contour-edge attachments made the filament read as an extruded corridor rather than pulled goo | Preserve the accepted shared field, continuity, sparse topology, close-range character, and field-pixel budget; round only the long-range centerline, taper, shoulders, and sampled edges |
| 2026-09-07 | Harmonic Geometry rounded filament candidate implemented | Replaced the fixed 16-segment, shallow-bend, hard-ended corridor with a distance-curved centerline, broad shoulders inset into both prepared contours, quintic zero-slope width easing, and field-resolution-aware smooth edges. Each connection geometry is computed once per draw and shared by the color and visibility passes; it remains part of the same material field and keeps the threshold-safe waist | `a265fba`; final 15-test geometry/lifecycle gate, type-check, build, and diff check pass after a preceding 63-test adjacent gate. Chrome far-span captures show curved shoulders and sides with the former angular ends removed; fine stair-stepping at an extreme 2000px span remains the existing capped-field limit. Fresh Astra Low review approved Vercel review, identified the duplicate per-frame geometry work, and confirmed its repair plus lineage re-audit. Exact visual acceptance and real-device Safari performance remain open; PR #32 is the review surface |
| 2026-09-07 | BJS-117 Harmonic Geometry PR feedback corrected | Closed the production-replacement contradictions around the canvas Unique: held notes survive display trimming, enable-time state hydrates from live notes, mount-time state recreates missing blob anchors without replaying audio/effects, finite id-less playback expires, released analysis anchors expire with their blobs, and legacy releases retain separate chromatic-analysis and solfege-blob keys. Geometry now stops with Blobs, tolerates a transient missing anchor, sorts around its own centroid, labels triads, constrains phone-width text, and mirrors enabled learning labels through a polite accessible DOM companion. Merge caches parents across drift and reconnects held bodies before attaching releasing bodies. The unused `FloatingPopup.vue` and its dead Animation Duration field/preset were removed; persisted values are dropped by the existing allowlisted migration, while the already-migrated Connection Softness control remains the field/line softness owner | PR #32 review follow-up. 102 focused/adjacent tests across 11 suites, type-check, production build, and diff check pass. A fresh Astra lineage audit found the mount-time anchor gap, confirmed its repair, and reported no other blockers. Existing Browserslist, soundfont-eval, mixed instrument import, and bundle-size warnings remain. Exact Merge/Outline visual acceptance and real-device Safari performance remain open on the Vercel review surface |
| 2026-09-07 | Harmonic Geometry reduced to Merge and Web | Burooj accepted exactly two presentations and applying Merge's material integration to Web. Merge means one shared body. Web means distinct blobs joined by the full analyzed harmonic graph as color-interpolated, contour-to-contour field filaments, with stronger perimeter strands, finer/dimmer interior strands, fused intersections, long-distance continuity, and stable treatment through drift/releases. Labels remain independently optional and off by default | Retire Outline and Center Only, default new geometry to Merge, migrate persisted Outline to Merge and Center Only to Web, and make production plus the real guide cross the shared field seam |
| 2026-09-07 | Harmonic Geometry fused-field Web candidate implemented | Web now consumes the existing analyzed boundary/interior topology inside `useBlobFieldRenderer`; tapered curved ribbons enter the exact prepared contours and share the same blur, threshold, color interpolation, and capped field as the bodies. The former stroke/glow/backdrop renderer is removed. Surviving edge roles persist when released bodies disappear, while new note membership deliberately replans the perimeter. Dense twelve-note color is accumulated in non-saturating eight-layer batches into float buffers; sub-quantum tails become transparent rather than black. The guide exercises four bodies and both interior diagonals. Config exposes only Merge/Web with the accepted legacy mappings and Merge default | `e5d1421`, `1b45bd3`, `60102a4`; 90 focused/adjacent tests across 7 suites, type-check/build, and diff check pass. Fresh Astra Low lineage/correctness audit found and confirmed both lifecycle-role and dense-color repairs, with no blocker. Existing build warnings remain; two untouched legacy canvas suites still fail resolution of their stale `@/helpers/test-utils` imports. Collaborative preview opened but its local navigation/snapshot automation failed, so exact Web finish, actual raster evidence, maximum-density browser timing, and real-device Safari performance remain on PR #32's Vercel review gate |
| 2026-09-07 | Blob material continuity across Harmony implemented | Burooj identified the renderer handoff directly: one-note Web used ordinary Blob blur/glow, then adding a relationship replaced the whole body with Harmony's field finish; Web filaments also inherited high-frequency contour jitter. Web now remains in the shared field even before an analyzed edge exists, that field receives Blob blur and renders a color-preserving glow from its own pixels, and its relationship geometry attaches to stable rendered radii while Merge retains its accepted contour attachment. The previously inert Oscillation Amplitude now controls one correctly applied slow breathing scale and is named Breathing Amplitude; Vibration is clarified as percentage-based Edge Vibration | `a3bbabc`; 93 focused/adjacent tests across 7 suites, type-check, production build, and diff check pass. Fresh Astra Low correctness/lineage audit found no blocker and caught the corrected percentage label. Harmony softness/threshold still shapes the shared field, single-note Web now pays that capped field cost, and enabled glow adds one filtered composite pass; deployed transition quality, maximum-density timing, and real-device Safari performance remain on PR #32's review gate. Collaborative preview navigation failed again, so no local browser visual claim is made |
| 2026-09-07 | Blob and Harmony configuration unified | Burooj accepted removing the artificial ownership split: one Blob master and one persisted Blob schema now govern appearance, motion, relationships, analysis, and labels. The Config panel has one grouped Blobs section; Connections is Off by default with Merge/Web choices, Field Softness and Fusion Strength replace Harmony-owned names, Web and Label opacity are independent, and Analysis Hold Time replaces the two controls whose runtime only used their sum. Production analysis/rendering, presets, imports, saved configurations, snapshots, and the real guide consume the same canonical Blob values. The migration preserves canonical values over legacy ones, maps old modes, timing, note limits, labels, softness, fusion, and shared opacity, then drops `floatingPopup` from canonical exports | `5ddf22a`; 100 focused/adjacent tests across 8 suites, type-check, production build, and diff check pass. Fresh Astra Low review found no implementation blocker. A disabled legacy Web preference necessarily becomes Off because the accepted single enum has no separate remembered-mode state; turning the Blob master off also clears released-note hold history while currently held notes rehydrate on enable. Collaborative preview navigation remained unavailable, so grouped-control and final material acceptance stay on PR #32's Vercel review surface |
| 2026-09-07 | BJS-117 final open review threads addressed | Re-auditing every unresolved PR thread found four current lifecycle/label gaps and one superseded popup-era expectation. Sustained blobs no longer disappear after an arbitrary ten-second cap; released analysis anchors expire with the earlier of visual fade or scale exit; whole-harmony chord/emotion labels are suppressed while a partial anchored scene remains; and narrow canvas labels wrap or truncate without horizontal glyph compression. Merge deliberately continues following the Blob body's release material instead of the retired popup Hide Delay, while Analysis Hold Time owns analysis retention | `bf83e59`; 101 focused/adjacent tests across 8 suites, type-check through production build, and diff check pass. Fresh Astra Low standards/spec reviews found no additional code issue; the spec review identified only the stale PR description, which is corrected before merge. Existing dependency/chunk warnings remain. Deployed visual acceptance, maximum-density browser timing, and real-device Safari performance remain explicit review gates |
| 2026-09-07 | Tabs visual acceptance | Burooj selected the continuous chip-slide treatment over production's separate expanding footer tabs and accepted one stable page-load edition drawn from the guide's chip variants, matching the Knob edition model; explicit guide variants stay fixed | Acceptance inspected on the focused `/style-guide/tabs` comparison surface against the real `ChipTabs.vue` and `TabbedOverlayPanel.vue` seams, including the sixteen-destination Config footprint. Formalization, production adoption, and verification remain open |
| 2026-09-07 | Tabs formalization and adoption | `Tabs.vue` is the sole Tabs presentation owner: the moving chip, continuous rail, compact/equal layouts, overflow containment, selected/disabled state, and six-edition page-load cycle now feed the real guide specimen plus Instrument and Config through `TabbedOverlayPanel`. The old Tabs visual family was retired; `TabsContent` retains only controlled content coordination. The focused `/style-guide/tabs` route remains an async, design-scoped inspection surface | `105feb7`, `580096f`; 27/27 focused tests, type-check, and production build pass. T3 preview verifies the focused guide and production Config at wide and narrow widths, including internal overflow and live content switching. Fresh bounded re-audit passed; existing build warnings remain |
| 2026-09-07 | Instrument Picker definition, adoption, and verification | Burooj accepted the loose Sticker library and rejected Badge variants. Production `InstrumentSelector.vue` owns the complete interior through Ivory Sticker choices wrapped in native buttons while preserving Drawer, Tabs, search, grouping, selection/audio flows, warmup/error handling, and close behavior. The dedicated guide mounts the same source through controlled selection | `6d98d94`, `8fc09f8`; focused tests, type-check, production build, visual verification, and fresh bounded lineage audit pass. Next top-menu unit: Config Menu |
| 2026-09-08 | Instrument Picker material, order, and icon correction | Burooj prohibited brass in picker Stickers, required current/warming and already-warmed sounds ahead of cold sounds, and extended Drawer-handle instrument identity into every choice Sticker. One shared resolver supplies family-specific icons with AudioWaveform fallback; Badge remains absent | `f65870b` plus post-audit coverage repair. 25/25 focused tests, type-check, production build, diff check, T3 production verification, and fresh bounded lineage re-audit pass. Next top-menu unit: Config Menu |
| 2026-09-08 | PR #47 review corrections | App now exposes one async `StyleGuide.vue` entry for the base and accepted focused pages; Tabs keeps late active destinations visible after rail contraction by observing both scrollport and track | 26/26 focused tests, type-check, production build, diff check, T3 verification, and fresh bounded lineage re-audit pass. Existing build warnings remain |
| 2026-09-08 | Config Menu definition and first candidate | Burooj accepted Knobs as the persistent setting language, an open field without the rounded tray, quiet metadata groups, capped responsive density, local Section controls beside the active section, global utilities in the top rail, shared Buttons for icon actions, and Ivory Sticker-faced Scene/Preset actions with no Badge or brass | Production `ConfigPanel.vue` owns the candidate and `/style-guide/config-menu` mounts it directly. The branch first merged current `origin/main` so the candidate preserves the canonical grouped Blob schema and conditional visibility. Focused Config/App tests and type-check pass; T3 DOM evidence at 1402px and 390px verifies 8/2-column Knob grids, five Blob groups, six Scene Stickers, zero Badge/brass Stickers, and zero document overflow. Exact visual acceptance and final production verification remain open |
| 2026-09-08 | Config Menu accepted and closed | Burooj accepted the candidate, corrected clipped labels and noisy floating-point readouts, and assigned Brass to specific Knobs. Astra Low recommended exactly the global Visuals and active-section Boolean masters: two scopes of enable/bypass meaning, with every adjustable field remaining Ivory and no Boolean drag follower to introduce Badge. The shared Knob seam now wraps labels and caps formatted display precision at two decimals without rounding interaction, emissions, or persistence | `03dc143`; 42/42 focused Knob/Config/Control Bar/App tests, type-check, production build, and diff check pass. T3 production/focused-source verification at 1402x877 and 427x925 covers Blobs, Scenes/Presets, the disabled state, and MIDI; it confirms 8/2-column layouts, clean multi-line long labels through the bottom Blob groups, exactly two Brass faces in configurable sections, zero Badge, zero horizontal overflow, and an exercised range follower showing `1.1s`. Existing Browserslist, soundfont-eval, mixed-import, and bundle-size warnings remain |
| 2026-09-08 | Top-menu material and gesture correction | Burooj asked the two Config Brass masters to use sheen plus glow, reduced the visible ladder of in-between Ink/Ivory shades, and made both the tab rail and menu body swipeable. Brass Boolean centers consume the accepted global instrument-metal finish while Knob meters add directional metal light and glow. Top Drawer, panel chrome, Tabs, and Config metadata now favor workhorse Ink/Ivory with controlled opacity. `Tabs.vue` owns direct drag-scrolling without release selection; `TabbedOverlayPanel.vue` owns adjacent enabled-tab swipes, vertical-axis arbitration, Knob/input exclusions, and post-swipe click suppression, so Config and Instrument Picker share one behavior | `edfbd2d`; 64/64 focused Tabs/Tabbed Panel/Knob/Config/Instrument Picker/Drawer tests, type-check, production build, and diff check pass. T3 at 427x925 exercises Config rail drag from `0` to `270px`, selection of an exposed offscreen Color tab, content swipe from Color to Scope, inherited Instrument Picker swipe from All to Keyboards, Ink top-menu surfaces, zero document overflow, and live brass gradient, `brass-sheen` animation, and `--shadow-glow-brass`; wide recording `browser-recording-mtsjlq7o` captures the shared production/focused source switching from Blobs to Glow. Existing build warnings remain |
| 2026-09-08 | Joystick latch confirmation refined | Burooj made the previously timing-only latch legible: a short directional drag commits with a firmer haptic and briefly retained Ivory direction readout, while a stationary tap on a latched direction returns Harmony to the automatic center. Held drag remains a momentary override and an already automatic stationary tap remains inert. Joystick owns the state transition, the shared haptic utility owns the distinct pulse, and DragValue composes the existing filled Ivory Sticker treatment without weakening the brass-only Badge taxonomy | `5d7e475`; Joystick, Control Bar, Drawer, and haptic coverage passes 53/53, with type-check, production build, and diff check green. The shared source remains consumed by production and the real guide. Collaborative preview reached the worktree but became unresponsive after mount, so no fresh live visual, physical-haptic, touch-hardware, or speaker-output claim is made |
| 2026-09-08 | Joystick latch confirmation corrected to an Ivory Badge | Burooj clarified that the committed state must become the actual Badge treatment, not a filled Sticker with an Ivory tone. DragValue now preserves the filled Ivory Sticker for ordinary Knob feedback while giving Joystick latch confirmation fixed Badge anatomy: an Ivory body with Ink edge and text. Brass remains the default Badge signal material; Ivory is the constrained committed-latch state | `1afbaac`; focused Joystick, Sticker, and Knob coverage passes 40/40, with type-check, production build, and diff check green. The real Sticker guide now shows only the accepted Brass and Ivory Badge materials. Collaborative browser inspection of a live short-drag latch confirms `sticker--badge sticker--color-ivory`, an Ivory body (`rgb(244, 239, 230)`), and Ink badge text (`rgb(10, 9, 8)`). No physical-haptic or touch-hardware claim is made |
| 2026-09-08 | Final chord-row accessibility and exact-color lineage repaired | Exact-head PR review exposed three remaining cross-surface contradictions: controlled Reduced Motion and Forced Colors omitted fused Chord faces, Harmony policy still called every stationary Joystick tap a no-op, and Blob preparation discarded borrowed tones' exact chromatic identity before resolving body color. The Keyboard specimen now controls both melody and chord faces, the policy distinguishes automatic no-op from directional tap-to-Auto, and ActiveBlob carries `pitchClassIndex` through the shared pitch-aware color resolver | `f65a9f7`; 81 focused tests across Keyboard, Joystick, ChordKey, Blob lifecycle/field/geometry, exact-pitch visuals, and color-system suites pass, with type-check, production build, and diff check green. Broader Keyboard density, deployed controlled-state visual acceptance, and physical touch/haptic verification remain open as already recorded |
| 2026-09-08 | Keyboard focus and asynchronous owner lifecycle closed | Exact-head review found that Space/Enter release could land in the other Keyboard focus zone and miss its original owner, while ordinary melody attacks did not receive the cancellation predicate already used by exact chord attacks. Activation-key release now belongs to the Keyboard root and checks both focus namespaces; pointer and QWERTY melody paths pass owner cancellation through `attackNoteWithOctave()` so a released slow attack is discarded before active state or `note-played` publication | `f99cf2c`; focused Keyboard, QWERTY, music-store, ChordKey, and voice-group coverage passes 55/55, with type-check, production build, and diff check green. The ownership policy and tracker record the shared chord/melody guarantee; hardware-key and slow-audio behavior remain test-backed rather than physical-device claims |
| 2026-09-08 | Borrowed playback identity and orphan focus retained | Exact-head review found that CodeStrip playback folded altered chord pitches onto an in-scale degree before publishing visual/MIDI events, and a focused held chord could fall back to the document body when a smaller-scale remap removed it on keyup. Strudel playback now emits borrowed pitch identity with `solfegeIndex: -1`, exact `pitchClassIndex`, and symmetric press/release context; Keyboard synchronously focuses the nearest surviving chord before deleting a released orphan snapshot | `34fff66`; focused Keyboard and superdough coverage passes 30/30, with type-check and diff check green. The implementation preserves the accepted chord surface and changes only downstream identity and focus lifecycle; production build and mainline integration follow in the PR gate |
| 2026-09-08 | Revised Mark family and Beat Indicator accepted | Burooj accepted the complete 28-glyph Mark family, confirmed Mark's production adoption through Stage particles, and accepted Beat Indicator as a production-owned compound even before a runtime composition selects its placement. Kicker retires completely; Sticker gains optional leading/trailing Mark content and Spine Card crosses that source directly. The temporary `/page` review workbench retires now that its decisions are closed | Preserve Stage Music Color, physics, and lifetime; preserve Beat's UIBeat integration dependency rather than inventing a runtime placement; migrate only Spine Card's existing section-marker anatomy without redesigning the separately owned card |
| 2026-09-08 | Mark, Beat Indicator, and marked Sticker handed off | The accepted Mark registry remains the single SVG/canvas authority and is production-adopted through Stage. Beat Indicator is an accepted production-owned compound with its runtime-consumer gate explicitly deferred to UIBeat. Sticker now owns optional leading/trailing Mark content, Spine Card consumes that seam directly, and `Kicker.vue` plus the temporary `/page` workbench are removed | `0ce4de7`, `a3d5fdb`, `9f8e1c2`; 100/100 focused and adjacent tests across 12 suites, type-check, production build, and diff check pass. Fresh bounded lineage audit found three stale `fleckShape` fixture fields and confirmed their removal with no other findings. T3 browser evidence on the Tailscale preview verifies production Stage after the loading transition, all 28 guide Marks, 13 Beat Indicator specimens, before/after marked Stickers, zero Kicker nodes, and `/page` no longer mounting rough-workbench DOM. Existing Browserslist, Strudel eval, mixed-import, and bundle-size warnings remain |
| 2026-09-08 | Chord row integrated with glissando and unified Marks | Current `main` brought root-managed continuous melody glissando plus the accepted Mark/Sticker consolidation into the chord-row branch. The merge preserves standalone Key input through an explicit managed-input boundary, reference-counts overlapping melody contacts while retaining late-attack cancellation, and scopes touch capture to melody rows so the overflowing chord row keeps horizontal pan. Sticker retains optional Mark composition alongside the constrained Ivory latch Badge; borrowed particle colors now use exact pitch identity with randomized canonical Marks | Merge `dbb1d15`; 97/97 focused tests across Keyboard, Key, ChordKey, Joystick, Sticker, Mark/particle, exact audio, music-store, voice-group, and App seams pass, with type-check, production build, and diff check green. Full suite reaches 702 passing tests and retains 10 unrelated legacy failures across missing imports, GSAP mocking, and utility expectations. Existing Browserslist, soundfont-eval, mixed-import, and bundle-size warnings remain; no new physical touch, haptic, MIDI, or speaker claim |
| 2026-09-08 | Borrowed identity retained through CodeStrip edits | Exact-head review found that manually replacing an in-scale absolute pitch with a borrowed pitch correctly changed playback but reparsed its Note or chord member through an in-scale degree fallback. The shared fallback now marks out-of-scale pitches with `scaleIndex: -1`, retains their exact chromatic index, and enforces raw-label policy through both event shapes | `eec8f3b`; 37/37 adjacent CodeStrip tests pass, including new single-note and chord-member edit regressions, with type-check, production build, and diff check green. Existing Browserslist, soundfont-eval, mixed-import, and bundle-size warnings remain |
| 2026-09-07 | Bar Tape definition, adoption, and verification | Burooj accepted Bar Tape as the compact alternative to raw Strudel code: one borderless 4px timeline of a pattern's chronological musical events, sized proportionally by duration and filled by runtime Music Color. One authoritative primitive now replaces production Pattern Card's raw strip, drives the real guide specimen and both deferred Pattern Card sources, and drops the unused seven-seat scale, size, playhead, dim, and downbeat variants without changing pattern actions or state | `f04c1e2`, `e104ea7`; 3/3 focused tests, type-check, production build, and fresh bounded lineage audit pass. Tailscale Chromium verifies the focused dev-only workbench at 320/960px, the real guide specimen, and four production Pattern Cards at `/`: exact 4px height, duration ratios including 1/2x, 1 1/2x, and 2x, runtime colors, and no horizontal overflow. The full baseline-red suite still reports 62 files/541 tests passing and 11 files/13 tests failing or not collecting on unrelated stale imports, utility expectations, environment assumptions, and GSAP mocks. Branch `design/bar-tape` is not pushed; Pattern Card source reconciliation is next |
| 2026-09-07 | Card definition and formalization | Burooj accepted the first extracted CardShell recipe as `Card.vue`: one Ink-3 square shell, Ink-5 hairline, required notched edge label, optional mark/footer anchors, flexible consumer content, and a 4px Ivory-default spine. Burooj then corrected the taxonomy: colored spines are ordinary Card inputs, not Spine Card variants or a separate component; the obsolete CardShell and SpineCard sources and separate guide unit are retired. This supersedes the earlier Mark-lane assumption that Spine Card remained a separate pending unit; marked Sticker stays valid independently | `380b658`; 3/3 focused Card tests, type-check, production build, and diff check pass. Tailscale Chromium confirms six real Cards on the focused page and seven in the real guide, exact Ivory/tomato/pine/mustard spine inputs, no old candidate/CardShell/SpineCard DOM, and no overflow at the focused page's measured 390px viewport. Existing build warnings remain. Production adoption waits for Pattern Card to compose Card without changing pattern behavior or state |
| 2026-09-08 | Pattern Card accepted and adopted; Button rebound promoted | Burooj accepted Pattern Card as one Card-based compound with collapsed metadata + Bar Tape and expanded CodeStrip + actions. Final corrections remove the redundant active label and duplicate instrument/key metadata, place Open in Strudel right-most, and make deletion a visible two-tap trash-to-check confirmation. Default patterns remain undeletable; Pattern List owns active/list choreography in its own future PR. Boolean Knob's elastic response is promoted as `--dur-bounce` / `--ease-bounce` and adopted by non-brass Buttons, with Brass retaining its direct snap | 46 focused Pattern Card, Bar Tape, Card, Button, and patterns-store tests pass with type-check/build and diff check. Tailscale Chromium at 390px verifies both Pattern Card states, zero page overflow, non-duplicated metadata, right-most Strudel action, two-tap confirmation, absence of “Active · Strudel”, and distinct shared non-brass/Knob rebound versus Brass tap timing. Existing Browserslist, soundfont-eval, mixed-import, and bundle-size warnings remain. PR review/merge receipt follows in Git history; Pattern List is next |
| 2026-09-08 | Pattern Card Astra Low review resolved | Delete confirmation is bound to the armed pattern identity, deleting a focused user pattern selects a surviving fallback instead of leaving the deck front blank, and Button rebound now exists only inside `prefers-reduced-motion: no-preference` | 39/39 focused Pattern Card, Button, and patterns-store tests pass, including the identity-swap regression; type-check, production build, and diff check pass. All three PR #46 review threads are answered and resolved before merge |
| 2026-09-08 | Config control alignment and sizing corrected | Burooj's deployed phone review exposed two layout contradictions in the accepted Config composition: label-bearing Brass Boolean wrappers were centered as whole stacks beside face-only Buttons, and intrinsic center text could expand individual setting faces beyond the requested Knob host. Config now aligns the 32px Boolean faces with adjacent small Buttons while keeping labels below; the shared Instrument Control face yields to its requested host width so every field in one responsive grid is equal | `8804d88`; 41/41 focused Knob/Config/Control Bar/App tests, type-check, production build, and diff check pass. T3 computed geometry and screenshot evidence cover production `/` plus the shared focused source: at 427x925 every Strings setting face is 51.23px, at 1402x877 the focused faces are 63px and production faces 72px, both Brass action faces and adjacent Buttons are exactly 32px on one centerline, and neither viewport has document overflow. Fresh local lineage audit confirms Instrument Control owns face containment and Config alone owns its action-row composition. Existing build warnings remain |
| 2026-09-08 | Chord row integrated with Card and Pattern Card adoption | Current `main` replaced the old pattern surface with authoritative Card, Bar Tape, and Pattern Card sources while promoting Button rebound. The merge retains that architecture and routes borrowed pattern segments through their exact chromatic identity, preserving the chord-row branch's Music Color lineage. The legacy adapter test now isolates the new compound boundary instead of loading CodeStrip's audio runtime | Merge `da8d405`; 139/139 focused and adjacent tests pass across Pattern Card, Card, Bar Tape, Button, patterns store, Keyboard, ChordKey, Joystick, and CodeStrip, with type-check, production build, and diff check green. Existing Browserslist, soundfont-eval, mixed-import, and bundle-size warnings remain |
