---
phase: 01-instrument-trust-flow
plan: "04"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/stores/patterns.ts
  - src/__tests__/stores/patterns.test.ts
  - src/__tests__/stores/music-midi.test.ts
  - src/__tests__/components/visual/UnifiedVisualEffects.test.ts
  - src/__tests__/integration/note-event-contract.test.ts
autonomous: true
requirements:
  - FEEL-03
must_haves:
  truths:
    - Pattern capture records the instrument that was actually played, even if selection has already moved on while a new pack warms.
    - Pattern capture, visuals, and MIDI mirroring still react to the same shared `note-played` and `note-released` contract after readiness gating lands.
    - A warming-state early return emits no partial consumer-visible note events for downstream listeners to mis-handle.
  artifacts:
    - path: src/stores/patterns.ts
      provides: Pattern capture driven by emitted ready-instrument note payloads instead of selected-instrument guessing
    - path: src/__tests__/stores/patterns.test.ts
      provides: Pattern-capture regression coverage for selected-versus-ready instrument drift
    - path: src/__tests__/integration/note-event-contract.test.ts
      provides: Focused shared note-event contract verification across pattern capture, visuals, and MIDI mirroring
  key_links:
    - from: src/stores/music.ts
      to: src/stores/patterns.ts
      via: emitted note payload instrument and note identity
      pattern: note-played|note-released|instrument|noteId
    - from: src/stores/music.ts
      to: src/composables/useMidiControls.ts
      via: shared window event payload
      pattern: note-played|note-released
    - from: src/stores/music.ts
      to: src/components/UnifiedVisualEffects.vue
      via: shared window event payload
      pattern: note-played|note-released
---

<objective>
Lock the consumer side of the ready-instrument note contract so pattern capture, visuals, and MIDI mirroring keep trusting the same emitted events after readiness gating lands.

Purpose: Finish the remaining FEEL-03 seam from D-07 by removing direct selected-instrument guessing in pattern capture and proving the shared note event contract still holds for real consumers.
Output: A `patterns.ts` consumer that trusts emitted ready-instrument payloads plus focused contract tests for pattern capture, visuals, and MIDI listeners.
</objective>

<execution_context>
@/Users/burooj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/burooj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-instrument-trust-flow/01-CONTEXT.md
@.planning/codebase/CONCERNS.md
@src/stores/music.ts
@src/stores/patterns.ts
@src/composables/useMidiControls.ts
@src/components/UnifiedVisualEffects.vue
@src/__tests__/stores/patterns.test.ts
@src/__tests__/stores/music-midi.test.ts
@src/__tests__/components/visual/UnifiedVisualEffects.test.ts
@src/__tests__/integration/audio-visual-integration-fixed.test.ts

<interfaces>
From `src/stores/music.ts` after Plan 02:

```ts
window.dispatchEvent(new CustomEvent("note-played", {
  detail: {
    note,
    frequency,
    noteName,
    solfegeIndex,
    octave,
    noteId,
    mode,
    key,
    instrument,
  },
}));

window.dispatchEvent(new CustomEvent("note-released", {
  detail: {
    note,
    noteId,
    noteName,
    frequency,
    octave,
    mode,
    key,
    instrument,
  },
}));
```

From `src/stores/patterns.ts` today:

```ts
const liveInputMeta = computed(() => ({
  mode: musicStore.currentMode,
  key: musicStore.currentKey,
  instrument: instrumentStore.currentInstrument,
  bpm: resolveBpm(...),
}));

const partialLogNote: Partial<LogNote> = {
  ...
  instrument: instrumentStore.currentInstrument,
};
```

This plan must remove that selected-instrument coupling from pattern capture paths and make the consumer trust the emitted note payload instead.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Switch pattern capture to the emitted ready-instrument contract</name>
  <files>src/stores/patterns.ts, src/__tests__/stores/patterns.test.ts</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/01-instrument-trust-flow/01-CONTEXT.md
.planning/codebase/CONCERNS.md
src/stores/music.ts
src/stores/patterns.ts
src/stores/instrument.ts
src/__tests__/stores/patterns.test.ts
  </read_first>
  <behavior>
    - Test 1: `handleNotePressed()` records the `instrument` carried by the `note-played` event even when `instrumentStore.currentInstrument` has already moved to a newer selected-but-not-ready choice.
    - Test 2: `currentSketchMeta.instrument` follows the captured emitted instrument for live notes instead of regressing to direct selected-instrument reads during that capture flow.
    - Test 3: Existing desk semantics from D-09 through D-11 still hold for load, send, and delete once the captured instrument source changes.
  </behavior>
  <action>Address the checker blocker directly in `src/stores/patterns.ts`: stop writing `instrumentStore.currentInstrument` into note-capture paths that are supposed to trust the shared note-event contract. Use the `instrument` carried by `note-played` as the source of truth for pending notes and any live-capture meta needed to keep the current sketch coherent. If empty-state fallback meta still needs an instrument, prefer the ready/playable instrument contract from Plan 02 rather than the merely selected instrument. Keep `loadPatternAsBase()`, `sendCurrentPattern()`, and delete semantics from 01-01 intact; this task is about removing contract leakage, not changing desk behavior. Add a focused regression test in `src/__tests__/stores/patterns.test.ts` where the emitted instrument differs from the currently selected instrument and assert the captured pattern data follows the emitted one.</action>
  <acceptance_criteria>
    - `rg -n 'detail\\.instrument|readyInstrument|playableInstrument' src/stores/patterns.ts` returns matches in the capture path.
    - `rg -n 'selected-but-not-ready|emitted instrument|detail: \\{.*instrument' src/__tests__/stores/patterns.test.ts` returns matches or equivalent contract-specific assertions exist.
    - `bun run test:run src/__tests__/stores/patterns.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>bun run test:run src/__tests__/stores/patterns.test.ts</automated>
  </verify>
  <done>`patterns.ts` no longer guesses the played instrument from selected store state during note capture, so the ready-instrument contract has an actual consumer on the pattern side.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add focused downstream verification for the shared note event contract</name>
  <files>src/__tests__/stores/music-midi.test.ts, src/__tests__/components/visual/UnifiedVisualEffects.test.ts, src/__tests__/integration/note-event-contract.test.ts</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/01-instrument-trust-flow/01-CONTEXT.md
.planning/codebase/CONCERNS.md
src/stores/music.ts
src/stores/patterns.ts
src/composables/useMidiControls.ts
src/components/UnifiedVisualEffects.vue
src/__tests__/stores/music-live-notes.test.ts
src/__tests__/stores/music-midi.test.ts
src/__tests__/components/visual/UnifiedVisualEffects.test.ts
src/__tests__/integration/audio-visual-integration-fixed.test.ts
  </read_first>
  <behavior>
    - Test 1: A ready `musicStore.attackNote()` and `releaseNote()` cycle still drives pattern capture through `note-played` / `note-released` without losing `noteId`, `noteName`, `solfegeIndex`, `octave`, or `instrument`.
    - Test 2: The visual listener path still receives `note`, `frequency`, `noteId`, `octave`, `noteName`, `mode`, and `key` from the shared payload.
    - Test 3: The MIDI listener path still resolves mirrored note behavior from the shared event payload, and a warming-state early return emits nothing for downstream consumers to mis-handle.
  </behavior>
  <action>Create a focused contract test at `src/__tests__/integration/note-event-contract.test.ts` rather than leaning on the stale broad integration suite. Mount or stub only the real consumers needed to prove the contract: instantiate `usePatternsStore()` for pattern capture, mount `UnifiedVisualEffects.vue` with mocked canvas handlers for the visual listener, and mount a small harness that runs `useMidiControls()` with mocked MIDI access and keyboard drawer state so the window listeners are active without hardware. Drive the ready-instrument path through `musicStore.attackNote(0)` and `releaseNote(noteId)` and assert all three consumers react to the same shared payload. Tighten `src/__tests__/components/visual/UnifiedVisualEffects.test.ts` and `src/__tests__/stores/music-midi.test.ts` where direct consumer assertions make the contract clearer, and add a no-event assertion for the warming early-return path from Plan 02.</action>
  <acceptance_criteria>
    - `test -f src/__tests__/integration/note-event-contract.test.ts` succeeds.
    - `rg -n 'note-played|note-released|usePatternsStore|useMidiControls|UnifiedVisualEffects' src/__tests__/integration/note-event-contract.test.ts` returns matches.
    - `bun run test:run src/__tests__/stores/music-midi.test.ts src/__tests__/components/visual/UnifiedVisualEffects.test.ts src/__tests__/integration/note-event-contract.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>bun run test:run src/__tests__/stores/music-midi.test.ts src/__tests__/components/visual/UnifiedVisualEffects.test.ts src/__tests__/integration/note-event-contract.test.ts</automated>
  </verify>
  <done>The downstream consumers now have focused regression coverage, so readiness changes in `music.ts` cannot silently break pattern capture, visuals, or MIDI mirroring.</done>
</task>

</tasks>

<verification>
- Run the updated pattern-store regression test.
- Run the focused contract tests for MIDI, visuals, and the new integration harness.
- Run `bun run type-check` after both tasks complete so the shared note-event contract remains type-safe across stores and listeners.
</verification>

<success_criteria>
- `patterns.ts` captures the actual played instrument from emitted note payloads instead of selected-store guessing.
- The shared `note-played` and `note-released` contract remains stable for pattern capture, visuals, and MIDI listeners.
- The new focused integration test catches consumer-side contract drift without relying on the stale broad integration suite.
</success_criteria>

<output>
After completion, create `.planning/phases/01-instrument-trust-flow/01-04-SUMMARY.md`
</output>
