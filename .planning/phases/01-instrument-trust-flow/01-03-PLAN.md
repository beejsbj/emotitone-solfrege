---
phase: 01-instrument-trust-flow
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - src/components/DrawerKeyboard.vue
  - src/App.vue
  - src/style.css
  - src/__tests__/components/keyboard/DrawerKeyboard.test.ts
  - src/__tests__/components/core/App.test.ts
autonomous: false
requirements:
  - FEEL-02
  - FEEL-04
must_haves:
  truths:
    - User sees the keyboard/live-strip workspace visibly guarded while the chosen instrument is still warming.
    - User can use the main instrument surface without body-level scrolling or page stretch during normal play.
    - Intentional local scroll regions still work after body scroll is locked down.
  artifacts:
    - path: src/components/DrawerKeyboard.vue
      provides: Workspace warming guard and desaturated working-area state
    - path: src/App.vue
      provides: Viewport-bounded shell container for the instrument
    - path: src/style.css
      provides: Document-level overflow lock for normal play
    - path: src/__tests__/components/keyboard/DrawerKeyboard.test.ts
      provides: Guard visibility verification
  key_links:
    - from: src/components/DrawerKeyboard.vue
      to: src/stores/instrument.ts
      via: warming guard state
      pattern: isWarmingInstrument|warmingInstrument
    - from: src/App.vue
      to: src/style.css
      via: viewport-bounded root shell
      pattern: overflow-hidden|overscroll-behavior
    - from: src/style.css
      to: src/components/patterns/LiveStrip.vue
      via: preserve local scroll while locking body
      pattern: overflow hidden on body, local overflow remains in component files
---

<objective>
Guard the live workspace while a new instrument warms and remove accidental document scrolling without flattening intentional local scroll regions.

Purpose: Finish D-05, D-06, and D-12 through D-14 by making the keyboard/live-strip workspace feel contained and trustworthy.
Output: A working-area warming guard, viewport-bounded shell layout, and document-level overflow rules that eliminate browser-page scrolling during play.
</objective>

<execution_context>
@/Users/burooj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/burooj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-instrument-trust-flow/01-CONTEXT.md
@src/components/DrawerKeyboard.vue
@src/App.vue
@src/style.css
@src/components/keyboard/KeyboardActionBar.vue
@src/components/patterns/LiveStrip.vue
@src/stores/instrument.ts

<interfaces>
From `src/components/DrawerKeyboard.vue` today:

```ts
<PatternList />
<LiveCard />
<KeyboardActionBar />
...
<KeyboardKey ... />
```

Plan 02 is expected to expose readable instrument readiness refs from `src/stores/instrument.ts`; this plan should consume those refs directly rather than duplicating readiness logic inside the component tree.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a warming guard to the live working area</name>
  <files>src/components/DrawerKeyboard.vue, src/__tests__/components/keyboard/DrawerKeyboard.test.ts</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/01-instrument-trust-flow/01-CONTEXT.md
src/stores/instrument.ts
src/components/DrawerKeyboard.vue
src/components/patterns/LiveCard.vue
src/components/keyboard/KeyboardActionBar.vue
src/__tests__/components/keyboard/DrawerKeyboard.test.ts
  </read_first>
  <behavior>
    - Test 1: When the instrument store reports warming, `DrawerKeyboard` renders a visible guard with the warming instrument name and a non-ready message.
    - Test 2: The guard disappears when warming ends, leaving the existing pattern list, live card, and keyboard grid intact.
    - Test 3: The guard is implemented inside the working-area surface, not as a global splash replacement.
  </behavior>
  <action>Per D-05 and D-06, consume the readiness refs introduced in Plan 02 and render a guard layer inside `DrawerKeyboard.vue` that covers the pattern list, live strip, action controls, and keyboard grid while the selected instrument is still warming. Use concrete hooks `data-testid="instrument-warming-guard"` and a root modifier class such as `drawer--warming`. Include explicit copy like `Warming {instrument}…` and `Selected, not ready yet.` Apply a desaturated or muted class to the underlying working area while the guard is present, but keep the underlying component structure unchanged so the guard lifts cleanly. Create `src/__tests__/components/keyboard/DrawerKeyboard.test.ts` with stubs for child components and assertions for guard-present and guard-absent states.</action>
  <acceptance_criteria>
    - `rg -n 'data-testid="instrument-warming-guard"|drawer--warming|Selected, not ready yet' src/components/DrawerKeyboard.vue` returns matches.
    - `test -f src/__tests__/components/keyboard/DrawerKeyboard.test.ts` succeeds.
    - `bun run test:run src/__tests__/components/keyboard/DrawerKeyboard.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>bun run test:run src/__tests__/components/keyboard/DrawerKeyboard.test.ts</automated>
  </verify>
  <done>The live working area visibly belongs to the warming state, and the guard lives exactly where the user is trying to play rather than hiding in unrelated shell chrome.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock document scrolling while preserving local surface scrolling</name>
  <files>src/App.vue, src/style.css, src/__tests__/components/core/App.test.ts</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/01-instrument-trust-flow/01-CONTEXT.md
src/App.vue
src/style.css
src/components/DrawerKeyboard.vue
src/components/keyboard/KeyboardActionBar.vue
src/components/patterns/LiveStrip.vue
src/__tests__/components/core/App.test.ts
  </read_first>
  <behavior>
    - Test 1: The ready app shell renders inside a viewport-bounded overflow-hidden container instead of a document-stretching `min-h-screen` stack.
    - Test 2: Global CSS locks document overflow and overscroll behavior on `html`, `body`, and `#app`.
    - Test 3: The plan leaves intentional local scroll surfaces alone at the code level, including the keyboard action strip and live strip notation/editor containers.
  </behavior>
  <action>Per D-12 through D-14, update `src/style.css` so `html`, `body`, and `#app` form a viewport-bounded shell with `height: 100%` or `100dvh`, `overflow: hidden`, and `overscroll-behavior: none` during normal play. In `src/App.vue`, replace the current ready-shell `min-h-screen` container with a concrete viewport-safe wrapper such as `h-dvh overflow-hidden flex flex-col`, so the fixed drawer no longer stretches the document and creates a body scrollbar. Do not remove or weaken the local `overflow-x: auto`, `overflow-y: auto`, `touch-action: pan-x`, or notation scroller behavior in `KeyboardActionBar.vue` and `LiveStrip.vue`; those local scroll regions are explicitly allowed by D-13. Update `src/__tests__/components/core/App.test.ts` so the ready-shell assertion matches the new root classes.</action>
  <acceptance_criteria>
    - `rg -n '^html|^body|^#app|overflow: hidden|overscroll-behavior' src/style.css` returns matches for document-level locking.
    - `rg -n 'overflow-hidden|h-dvh|h-screen' src/App.vue` returns the new viewport-bounded shell class.
    - `rg -n 'overflow-x: auto|touch-action: pan-x pinch-zoom' src/components/keyboard/KeyboardActionBar.vue` returns matches, confirming the control strip still owns horizontal scrolling.
    - `rg -n 'overflow-x: auto' src/components/patterns/LiveStrip.vue` returns matches, confirming the live strip still owns a local horizontal scroll surface.
    - `bun run test:run src/__tests__/components/core/App.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>bun run test:run src/__tests__/components/core/App.test.ts</automated>
  </verify>
  <done>The browser page stops scrolling during normal instrument use, while intentional internal scroll regions remain available inside the desk surfaces.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify local horizontal scroll stays local to the working surfaces</name>
  <files>src/App.vue, src/style.css, src/components/keyboard/KeyboardActionBar.vue, src/components/patterns/LiveStrip.vue</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/01-instrument-trust-flow/01-CONTEXT.md
src/App.vue
src/style.css
src/components/keyboard/KeyboardActionBar.vue
src/components/patterns/LiveStrip.vue
  </read_first>
  <action>Use the built Phase 1 shell in a narrow viewport to confirm the new body overflow lock does not swallow the intentional local horizontal scroll regions called out in D-13. This checkpoint is required because grep and unit tests can prove the CSS hooks remain present, but they cannot prove touch/drag behavior still feels local in the browser.</action>
  <acceptance_criteria>
    - In a narrow viewport, dragging sideways on the keyboard control strip scrolls only that strip and does not move the page.
    - After the live strip overflows, dragging sideways on the notation/editor scrolls only that surface and does not move the page.
    - No body scrollbar appears during either interaction.
  </acceptance_criteria>
  <what-built>Viewport-bounded shell overflow rules plus preserved local scroll surfaces in `KeyboardActionBar.vue` and `LiveStrip.vue`.</what-built>
  <how-to-verify>
    1. Run `bun run dev` and open the app in a narrow mobile-width viewport.
    2. Drag sideways on the instrument/config control strip and confirm the strip scrolls horizontally while the page stays fixed.
    3. Play or load enough notes to make the live strip overflow, then drag sideways on the notation/editor and confirm that surface scrolls while the page stays fixed.
    4. Confirm no page-level scrollbar appears during either interaction.
  </how-to-verify>
  <verify>
    <automated>rg -n 'overflow-x: auto|touch-action: pan-x pinch-zoom' src/components/keyboard/KeyboardActionBar.vue && rg -n 'overflow-x: auto' src/components/patterns/LiveStrip.vue</automated>
  </verify>
  <done>Manual verification confirms both local horizontal scroll regions still work after the shell overflow lock lands.</done>
  <resume-signal>Type "approved" if both local scroll surfaces still work, or describe which surface failed.</resume-signal>
</task>

</tasks>

<verification>
- Run the new `DrawerKeyboard` test and the updated `App` shell test.
- Run the code-level scroll-preservation checks for `KeyboardActionBar.vue` and `LiveStrip.vue`.
- Complete the local-scroll checkpoint in a narrow viewport before marking the plan done.
- Run `bun run type-check` after both tasks complete to confirm the new guard state and shell class changes stay type-safe.
</verification>

<success_criteria>
- `DrawerKeyboard.vue` renders a working-area warming guard driven by the instrument store.
- `style.css` and `App.vue` lock page-level scrolling without flattening local scroll surfaces.
- Phase 1 no longer relies on accidental body scrolling as part of the instrument layout.
</success_criteria>

<output>
After completion, create `.planning/phases/01-instrument-trust-flow/01-03-SUMMARY.md`
</output>
