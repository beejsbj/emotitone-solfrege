diff --git a/dist/index.mjs b/dist/index.mjs
index f973c75..790e3a9 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -157,8 +157,8 @@ const gm = {
   ],
   gm_vibraphone: [
     // Vibraphone: Chromatic Percussion
-    "0110_JCLive_sf2_file",
     "0110_Aspirin_sf2_file",
+    "0110_JCLive_sf2_file",
     "0110_Chaos_sf2_file",
     "0110_FluidR3_GM_sf2_file",
     "0110_GeneralUserGS_sf2_file",
@@ -470,8 +470,8 @@ const gm = {
   ],
   gm_acoustic_bass: [
     // Acoustic Bass: Bass
-    "0320_JCLive_sf2_file",
     "0320_FluidR3_GM_sf2_file",
+    "0320_JCLive_sf2_file",
     "0320_Aspirin_sf2_file",
     "0320_Chaos_sf2_file"
     // 0320_GeneralUserGS_sf2_file // missing notes
@@ -517,8 +517,8 @@ const gm = {
   ],
   gm_slap_bass_1: [
     // Slap Bass 1: Bass
-    "0360_Aspirin_sf2_file",
     "0360_JCLive_sf2_file",
+    "0360_Aspirin_sf2_file",
     "0360_FluidR3_GM_sf2_file",
     "0360_Chaos_sf2_file"
     //0360_GeneralUserGS_sf2_file // _1 oct
@@ -816,8 +816,8 @@ const gm = {
   ],
   gm_tuba: [
     // Tuba: Brass
-    "0580_FluidR3_GM_sf2_file",
     "0580_Aspirin_sf2_file",
+    "0580_FluidR3_GM_sf2_file",
     "0580_Chaos_sf2_file",
     "0580_GeneralUserGS_sf2_file"
     //0580_JCLive_sf2_file // _1oct
@@ -965,10 +965,10 @@ const gm = {
   ],
   gm_bassoon: [
     // Bassoon: Reed
-    "0700_JCLive_sf2_file",
     //0700_Aspirin_sf2_file // detune + gain diffs
     // 0700_Chaos_sf2_file // detune + crackles
     "0700_FluidR3_GM_sf2_file",
+    "0700_JCLive_sf2_file",
     "0700_GeneralUserGS_sf2_file",
     //0700_SBLive_sf2
     //0700_SoundBlasterOld_sf2
@@ -1821,88 +1821,133 @@ async function loadFont(name) {
   const load = async () => {
     const url = `${soundfontUrl}/${name}.js`, preset = await fetch(url).then((e) => e.text());
     let [_, data] = preset.split("={");
-    return eval("{" + data);
+    const parsedPreset = eval("({" + data.trim().replace(/;$/, "") + ")");
+    return parsedPreset.zones ?? parsedPreset;
   };
-  return loadCache[name] = load(), loadCache[name];
+  return loadCache[name] = load().catch((e) => {
+    throw delete loadCache[name], e;
+  }), loadCache[name];
 }
-async function getFontBufferSource(e, l, s) {
-  let { note: f = "c3", freq: a } = l, r;
-  if (a)
-    r = freqToMidi(a);
+async function getFontBufferSource(e, s, i) {
+  let { note: f = "c3", freq: n } = s, r;
+  if (n)
+    r = freqToMidi(n);
   else if (typeof f == "string")
     r = noteToMidi(f);
   else if (typeof f == "number")
     r = f;
   else
     throw new Error(`unexpected "note" type "${typeof f}"`);
-  const { buffer: n, zone: i } = await getFontPitch(e, r, s), G = s.createBufferSource();
-  G.buffer = n;
-  const u = i.originalPitch - 100 * i.coarseTune - i.fineTune, o = 1 * Math.pow(2, (100 * r - u) / 1200);
-  return G.playbackRate.value = o, i.loopStart > 1 && i.loopStart < i.loopEnd && (G.loop = !0, G.loopStart = i.loopStart / i.sampleRate, G.loopEnd = i.loopEnd / i.sampleRate), G;
+  const { buffer: a, zone: l } = await getFontPitch(e, r, i), G = i.createBufferSource();
+  G.buffer = a;
+  const u = l.originalPitch - 100 * l.coarseTune - l.fineTune, o = 1 * Math.pow(2, (100 * r - u) / 1200);
+  return G.playbackRate.value = o, l.loopStart > 1 && l.loopStart < l.loopEnd && (G.loop = !0, G.loopStart = l.loopStart / l.sampleRate, G.loopEnd = l.loopEnd / l.sampleRate), G;
 }
 let bufferCache = {};
-async function getFontPitch(e, l, s) {
-  const f = `${e}:::${l}`;
+const zoneBufferCache = /* @__PURE__ */ new WeakMap();
+async function loadZoneBuffer(e, s) {
+  let i = zoneBufferCache.get(e);
+  if (i || (i = /* @__PURE__ */ new WeakMap(), zoneBufferCache.set(e, i)), !i.get(s)) {
+    const f = getBuffer(e, s).then((n) => n || e.buffer).then((n) => {
+      if (!n)
+        throw new Error("no soundfont buffer decoded");
+      return n;
+    }).catch((n) => {
+      throw i.delete(s), n;
+    });
+    i.set(s, f);
+  }
+  return i.get(s);
+}
+async function prewarmSoundfont(e, s) {
+  const i = await loadFont(e);
+  if (!Array.isArray(i))
+    throw new Error(`Could not load soundfont ${e}`);
+  await Promise.all(i.map((f) => loadZoneBuffer(f, s)));
+}
+async function getPreparedSoundfont(e, s) {
+  const i = await loadFont(e);
+  if (!Array.isArray(i))
+    throw new Error(`Could not load soundfont ${e}`);
+  return Promise.all(i.map(async (f) => ({
+    buffer: await loadZoneBuffer(f, s),
+    originalPitch: f.originalPitch,
+    coarseTune: f.coarseTune,
+    fineTune: f.fineTune,
+    keyRangeLow: f.keyRangeLow,
+    keyRangeHigh: f.keyRangeHigh,
+    loopStart: f.loopStart,
+    loopEnd: f.loopEnd,
+    sampleRate: f.sampleRate
+  })));
+}
+async function getFontPitch(e, s, i) {
+  const f = `${e}:::${s}`;
   if (bufferCache[f])
     return bufferCache[f];
-  const a = async () => {
+  const n = async () => {
     const r = await loadFont(e);
     if (!r)
       throw new Error(`Could not load soundfont ${e}`);
-    const n = findZone(r, l);
-    if (!n)
-      throw new Error("no soundfont zone found for preset ", e, "pitch", l);
-    const i = await getBuffer(n, s);
-    if (!i)
-      throw new Error(`no soundfont buffer found for preset ${e}, pitch: ${l}`);
-    return { buffer: i, zone: n };
+    const a = findZone(r, s);
+    if (!a)
+      throw new Error("no soundfont zone found for preset ", e, "pitch", s);
+    const l = await loadZoneBuffer(a, i);
+    if (!l)
+      throw new Error(`no soundfont buffer found for preset ${e}, pitch: ${s}`);
+    return { buffer: l, zone: a };
   };
-  return bufferCache[f] = a(), bufferCache[f];
+  return bufferCache[f] = n().catch((r) => {
+    throw delete bufferCache[f], r;
+  }), bufferCache[f];
 }
-function findZone(e, l) {
-  return e.find((s) => s.keyRangeLow <= l && s.keyRangeHigh + 1 >= l);
+function findZone(e, s) {
+  return e.find((i) => i.keyRangeLow <= s && i.keyRangeHigh + 1 >= s);
 }
-async function getBuffer(e, l) {
+async function getBuffer(e, s) {
   if (e.sample) {
     console.warn("zone.sample untested!");
     const f = atob(e.sample);
-    e.buffer = l.createBuffer(1, f.length / 2, e.sampleRate);
-    const a = e.buffer.getChannelData(0);
-    let r, n, i;
-    for (var s = 0; s < f.length / 2; s++)
-      r = f.charCodeAt(s * 2), n = f.charCodeAt(s * 2 + 1), r < 0 && (r = 256 + r), n < 0 && (n = 256 + n), i = n * 256 + r, i >= 65536 / 2 && (i = i - 65536), a[s] = i / 65536;
+    e.buffer = s.createBuffer(1, f.length / 2, e.sampleRate);
+    const n = e.buffer.getChannelData(0);
+    let r, a, l;
+    for (var i = 0; i < f.length / 2; i++)
+      r = f.charCodeAt(i * 2), a = f.charCodeAt(i * 2 + 1), r < 0 && (r = 256 + r), a < 0 && (a = 256 + a), l = a * 256 + r, l >= 65536 / 2 && (l = l - 65536), n[i] = l / 65536;
   } else if (e.file) {
-    const f = e.file.length, a = new ArrayBuffer(f), r = new Uint8Array(a), n = atob(e.file);
-    let i;
-    for (let G = 0; G < n.length; G++)
-      i = n.charCodeAt(G), r[G] = i;
-    return new Promise((G) => l.decodeAudioData(a, G));
+    const f = e.file.length, n = new ArrayBuffer(f), r = new Uint8Array(n), a = atob(e.file);
+    let l;
+    for (let G = 0; G < a.length; G++)
+      l = a.charCodeAt(G), r[G] = l;
+    return new Promise(
+      (G, u) => s.decodeAudioData(n, G, u)
+    );
   }
 }
 function registerSoundfonts() {
-  Object.entries(gm).forEach(([e, l]) => {
+  Object.entries(gm).forEach(([e, s]) => {
     registerSound(
       e,
-      async (s, f, a) => {
-        const [r, n, i, G] = getADSRValues([
+      async (i, f, n) => {
+        const [r, a, l, G] = getADSRValues([
           f.attack,
           f.decay,
           f.sustain,
           f.release
-        ]), { duration: u } = f, o = getSoundIndex(f.n, l.length), C = l[o], S = getAudioContext(), t = await getFontBufferSource(C, f, S);
-        t.start(s);
-        const m = S.createGain(), U = t.connect(m), d = s + u;
-        getParamADSR(U.gain, r, n, i, G, 0, 0.3, s, d, "linear");
-        let F = d + G + 0.01;
-        const p = getVibratoOscillator(t.detune, f, s);
-        getPitchEnvelope(t.detune, f, s, d), t.stop(F);
-        const R = (M) => {
+        ]), { duration: u } = f, o = getSoundIndex(f.n, s.length), d = s[o], p = getAudioContext(), t = await getFontBufferSource(d, f, p);
+        t.start(i);
+        const h = p.createGain(), U = t.connect(h), S = i + u;
+        getParamADSR(U.gain, r, a, l, G, 0, 0.3, i, S, "linear");
+        let C = S + G + 0.01;
+        const m = getVibratoOscillator(t.detune, f, i);
+        getPitchEnvelope(t.detune, f, i, S), t.stop(C);
+        const R = (F) => {
+          C = Math.min(C, F), t.stop(C);
         };
         return onceEnded(t, () => {
-          releaseAudioNode(t), p?.stop(), a();
-        }), { node: U, stop: R, nodes: { source: [t], ...p?.nodes } };
+          releaseAudioNode(t), m?.stop(), n();
+        }), { node: U, stop: R, nodes: { source: [t], ...m?.nodes } };
       },
-      { type: "soundfont", prebake: !0, fonts: l }
+      { type: "soundfont", prebake: !0, fonts: s }
     );
   });
 }
@@ -3934,22 +3979,24 @@ const list = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty
   instrumentNames,
   instruments
 }, Symbol.toStringTag, { value: "Module" }));
-Pattern.prototype.soundfont = function(e, l = 0) {
-  return this.onTrigger((s, f, a, r) => {
-    const n = getAudioContext(), i = getPlayableNoteValue(s), G = e.presets[l % e.presets.length], u = r, o = [n, G, noteToMidi(i), u];
-    startPresetNote(...o)(u + s.duration);
+Pattern.prototype.soundfont = function(e, s = 0) {
+  return this.onTrigger((i, f, n, r) => {
+    const a = getAudioContext(), l = getPlayableNoteValue(i), G = e.presets[s % e.presets.length], u = r, o = [a, G, noteToMidi(l), u];
+    startPresetNote(...o)(u + i.duration);
   });
 };
 const soundfontCache = /* @__PURE__ */ new Map();
 function loadSoundfont(e) {
   if (soundfontCache.get(e))
     return soundfontCache.get(e);
-  const l = loadSoundfont$1(e);
-  return soundfontCache.set(e, l), l;
+  const s = loadSoundfont$1(e);
+  return soundfontCache.set(e, s), s;
 }
 export {
   getFontBufferSource,
+  getPreparedSoundfont,
   loadSoundfont,
+  prewarmSoundfont,
   registerSoundfonts,
   setSoundfontUrl,
   list as soundfontList,
diff --git a/fontloader.mjs b/fontloader.mjs
index b4ae598..6a3036d 100644
--- a/fontloader.mjs
+++ b/fontloader.mjs
@@ -28,9 +28,13 @@ async function loadFont(name) {
     const url = `${soundfontUrl}/${name}.js`;
     const preset = await fetch(url).then((res) => res.text());
     let [_, data] = preset.split('={');
-    return eval('{' + data);
+    const parsedPreset = eval('({' + data.trim().replace(/;$/, '') + ')');
+    return parsedPreset.zones ?? parsedPreset;
   };
-  loadCache[name] = load();
+  loadCache[name] = load().catch((error) => {
+    delete loadCache[name];
+    throw error;
+  });
   return loadCache[name];
 }

@@ -71,6 +75,58 @@ export async function getFontBufferSource(name, value, ac) {
 }

 let bufferCache = {};
+const zoneBufferCache = new WeakMap();
+
+async function loadZoneBuffer(zone, ac) {
+  let contextCache = zoneBufferCache.get(zone);
+  if (!contextCache) {
+    contextCache = new WeakMap();
+    zoneBufferCache.set(zone, contextCache);
+  }
+
+  if (!contextCache.get(ac)) {
+    const load = getBuffer(zone, ac)
+      .then((buffer) => buffer || zone.buffer)
+      .then((buffer) => {
+        if (!buffer) throw new Error('no soundfont buffer decoded');
+        return buffer;
+      })
+      .catch((error) => {
+        contextCache.delete(ac);
+        throw error;
+      });
+    contextCache.set(ac, load);
+  }
+
+  return contextCache.get(ac);
+}
+
+export async function prewarmSoundfont(name, ac) {
+  const preset = await loadFont(name);
+  if (!Array.isArray(preset)) {
+    throw new Error(`Could not load soundfont ${name}`);
+  }
+  await Promise.all(preset.map((zone) => loadZoneBuffer(zone, ac)));
+}
+
+// Expose the cached PCM and source tuning for renderers which prepare a bank
+// once. Do not create source nodes or expose the encoded preset payload.
+export async function getPreparedSoundfont(name, ac) {
+  const preset = await loadFont(name);
+  if (!Array.isArray(preset)) throw new Error(`Could not load soundfont ${name}`);
+  return Promise.all(preset.map(async (zone) => ({
+    buffer: await loadZoneBuffer(zone, ac),
+    originalPitch: zone.originalPitch,
+    coarseTune: zone.coarseTune,
+    fineTune: zone.fineTune,
+    keyRangeLow: zone.keyRangeLow,
+    keyRangeHigh: zone.keyRangeHigh,
+    loopStart: zone.loopStart,
+    loopEnd: zone.loopEnd,
+    sampleRate: zone.sampleRate,
+  })));
+}
+
 export async function getFontPitch(name, pitch, ac) {
   const key = `${name}:::${pitch}`;
   if (bufferCache[key]) {
@@ -86,13 +142,16 @@ export async function getFontPitch(name, pitch, ac) {
     if (!zone) {
       throw new Error('no soundfont zone found for preset ', name, 'pitch', pitch);
     }
-    const buffer = await getBuffer(zone, ac);
+    const buffer = await loadZoneBuffer(zone, ac);
     if (!buffer) {
       throw new Error(`no soundfont buffer found for preset ${name}, pitch: ${pitch}`);
     }
     return { buffer, zone };
   };
-  bufferCache[key] = load(); // dont await here to cache promise immediately!
+  bufferCache[key] = load().catch((error) => {
+    delete bufferCache[key];
+    throw error;
+  }); // dont await here to cache promise immediately!
   return bufferCache[key];
 }

@@ -136,7 +195,9 @@ async function getBuffer(zone, audioContext) {
         b = decoded.charCodeAt(i);
         view[i] = b;
       }
-      return new Promise((resolve) => audioContext.decodeAudioData(arraybuffer, resolve));
+      return new Promise((resolve, reject) =>
+        audioContext.decodeAudioData(arraybuffer, resolve, reject)
+      );
     }
   }
 }
@@ -171,7 +232,12 @@ export function registerSoundfonts() {
         getPitchEnvelope(bufferSource.detune, value, time, holdEnd);

         bufferSource.stop(envEnd);
-        const stop = (releaseTime) => {};
+        const stop = (releaseTime) => {
+          // Live voices shorten their original duration when a key or pulse
+          // releases. Keep the earliest stop so a later request cannot extend it.
+          envEnd = Math.min(envEnd, releaseTime);
+          bufferSource.stop(envEnd);
+        };
         onceEnded(bufferSource, () => {
           releaseAudioNode(bufferSource);
           vibratoHandle?.stop();
diff --git a/gm.mjs b/gm.mjs
index d8b7687..df2eea6 100644
--- a/gm.mjs
+++ b/gm.mjs
@@ -153,8 +153,8 @@ export default {
   ],
   gm_vibraphone: [
     // Vibraphone: Chromatic Percussion
-    '0110_JCLive_sf2_file',
     '0110_Aspirin_sf2_file',
+    '0110_JCLive_sf2_file',
     '0110_Chaos_sf2_file',
     '0110_FluidR3_GM_sf2_file',
     '0110_GeneralUserGS_sf2_file',
@@ -466,8 +466,8 @@ export default {
   ],
   gm_acoustic_bass: [
     // Acoustic Bass: Bass
-    '0320_JCLive_sf2_file',
     '0320_FluidR3_GM_sf2_file',
+    '0320_JCLive_sf2_file',
     '0320_Aspirin_sf2_file',
     '0320_Chaos_sf2_file',
     // 0320_GeneralUserGS_sf2_file // missing notes
@@ -513,8 +513,8 @@ export default {
   ],
   gm_slap_bass_1: [
     // Slap Bass 1: Bass
-    '0360_Aspirin_sf2_file',
     '0360_JCLive_sf2_file',
+    '0360_Aspirin_sf2_file',
     '0360_FluidR3_GM_sf2_file',
     '0360_Chaos_sf2_file',
     //0360_GeneralUserGS_sf2_file // _1 oct
@@ -812,8 +812,8 @@ export default {
   ],
   gm_tuba: [
     // Tuba: Brass
-    '0580_FluidR3_GM_sf2_file',
     '0580_Aspirin_sf2_file',
+    '0580_FluidR3_GM_sf2_file',
     '0580_Chaos_sf2_file',
     '0580_GeneralUserGS_sf2_file',
     //0580_JCLive_sf2_file // _1oct
@@ -961,10 +961,10 @@ export default {
   ],
   gm_bassoon: [
     // Bassoon: Reed
-    '0700_JCLive_sf2_file',
     //0700_Aspirin_sf2_file // detune + gain diffs
     // 0700_Chaos_sf2_file // detune + crackles
     '0700_FluidR3_GM_sf2_file',
+    '0700_JCLive_sf2_file',
     '0700_GeneralUserGS_sf2_file',
     //0700_SBLive_sf2
     //0700_SoundBlasterOld_sf2
diff --git a/index.mjs b/index.mjs
index 3179260..7126b57 100644
--- a/index.mjs
+++ b/index.mjs
@@ -1,6 +1,6 @@
-import { getFontBufferSource, registerSoundfonts, setSoundfontUrl } from './fontloader.mjs';
+import { getFontBufferSource, getPreparedSoundfont, prewarmSoundfont, registerSoundfonts, setSoundfontUrl } from './fontloader.mjs';
 import * as soundfontList from './list.mjs';
 import { startPresetNote } from 'sfumato';
 import { loadSoundfont } from './sfumato.mjs';

-export { loadSoundfont, startPresetNote, getFontBufferSource, soundfontList, registerSoundfonts, setSoundfontUrl };
+export { loadSoundfont, startPresetNote, getFontBufferSource, getPreparedSoundfont, prewarmSoundfont, soundfontList, registerSoundfonts, setSoundfontUrl };
