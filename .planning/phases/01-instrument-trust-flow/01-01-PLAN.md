---
phase: 01-instrument-trust-flow
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/keyboard/KeyboardActionBar.vue
  - src/components/patterns/LiveCard.vue
  - src/components/patterns/PatternList.vue
  - src/components/patterns/LiveStrip.vue
  - src/stores/patterns.ts
  - src/__tests__/components/keyboard/KeyboardActionBar.test.ts
  - src/__tests__/components/patterns/LiveCard.test.ts
  - src/__tests__/components/patterns/PatternList.test.ts
  - src/__tests__/stores/patterns.test.ts
autonomous: true
requirements:
  - FEEL-01
  - FEEL-03
must_haves:
  truths:
    - User can distinguish transport, edit, and commit actions at a glance from instrument/configuration controls within the keyboard working area.
    - User can backspace the active sketch and stay visually oriented around the deletion point.
    - User can send the current sketch into the pattern deck and continue with a cleared working line.
  artifacts:
    - path: src/components/keyboard/KeyboardActionBar.vue
      provides: Config-only keyboard control strip
    - path: src/components/patterns/LiveCard.vue
      provides: Live-strip transport and delete actions
    - path: src/components/patterns/PatternList.vue
      provides: Pattern-level send action
    - path: src/stores/patterns.ts
      provides: Desk mutation metadata for append, delete, load, and send
    - path: src/components/patterns/LiveStrip.vue
      provides: Mutation-aware orientation and local scroll behavior
  key_links:
    - from: src/components/patterns/LiveCard.vue
      to: src/composables/useLiveStrudelMirror.ts
      via: toggle/playback state
      pattern: useLiveStrudelMirror
    - from: src/components/patterns/PatternList.vue
      to: src/stores/patterns.ts
      via: pattern-level commit button
      pattern: sendCurrentPattern
    - from: src/components/patterns/LiveStrip.vue
      to: src/stores/patterns.ts
      via: mutation-aware scroll and deletion feedback
      pattern: lastSketchMutation|removeLastFromCurrentSketch
---

<objective>
Reshape the keyboard working area so instrument/configuration controls stay legible while transport, delete, and commit actions read as distinct sketch actions.

Purpose: Implement the control hierarchy and desk semantics from D-01 through D-03 and D-08 through D-11 without adding new capabilities.
Output: A config-only keyboard strip, live-strip action buttons, a pattern-level send action, and mutation-aware delete/send behavior in the sketch store and live strip.
</objective>

<execution_context>
@/Users/burooj/.codex/get-shit-done/workflows/execute-plan.md
@/Users/burooj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-instrument-trust-flow/01-CONTEXT.md
@src/components/keyboard/KeyboardActionBar.vue
@src/components/patterns/LiveCard.vue
@src/components/patterns/PatternList.vue
@src/components/patterns/LiveStrip.vue
@src/stores/patterns.ts
@src/composables/useLiveStrudelMirror.ts

<interfaces>
From `src/composables/useLiveStrudelMirror.ts`:

```ts
export interface LiveStrudelMirrorController {
  getCode: () => string;
  setCode: (code: string) => void;
  evaluate: () => Promise<void>;
  stop: () => Promise<void> | void;
}

return {
  toggle,
  currentCode,
  isPlaying,
  isReady,
  lastError,
  hasPlayableCode,
}
```

From `src/stores/patterns.ts`:

```ts
function loadPatternAsBase(patternId: string): void;
function sendCurrentPattern(): void;
function removeLastFromCurrentSketch(): void;

return {
  currentWorkingNotes,
  currentSketchNotes,
  currentSketchMeta,
  isStripCleared,
  loadPatternAsBase,
  sendCurrentPattern,
  removeLastFromCurrentSketch,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split config controls from live-strip actions</name>
  <files>src/components/keyboard/KeyboardActionBar.vue, src/components/patterns/LiveCard.vue, src/components/patterns/PatternList.vue, src/__tests__/components/keyboard/KeyboardActionBar.test.ts, src/__tests__/components/patterns/LiveCard.test.ts, src/__tests__/components/patterns/PatternList.test.ts</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/01-instrument-trust-flow/01-CONTEXT.md
src/components/keyboard/KeyboardActionBar.vue
src/components/patterns/LiveCard.vue
src/components/patterns/PatternList.vue
src/composables/useLiveStrudelMirror.ts
src/stores/patterns.ts
src/__tests__/components/keyboard/KeyboardActionBar.test.ts
  </read_first>
  <behavior>
    - Test 1: `KeyboardActionBar` still renders `Key`, `Mode`, `BPM`, `Octave`, `Rows`, and `Drawer`, but no longer renders `Undo`, `Play`, or `Send`.
    - Test 2: `LiveCard` renders compact icon-first controls for playback and backspace, and those controls call `toggle()` and `removeLastFromCurrentSketch()`.
    - Test 3: `PatternList` renders a pattern-level commit button that calls `sendCurrentPattern()` even when the deck is collapsed.
  </behavior>
  <action>Per D-01, D-02, and D-03, remove the `Undo`, `Play`, and `Send` knobs from `KeyboardActionBar.vue` so the keyboard working area clearly separates instrument/configuration controls from transport/edit/commit actions. Add compact action buttons to `LiveCard.vue` with concrete hooks `data-testid="live-strip-action-play"` and `data-testid="live-strip-action-backspace"`; use existing `useLiveStrudelMirror()` state for play/stop and existing `patternsStore.removeLastFromCurrentSketch()` for delete. Add a single pattern-level commit control in `PatternList.vue` with `data-testid="pattern-send-button"` and commit copy that preserves the typewriter/new-line feeling from D-11, rather than placing send back in the control row. Keep the existing horizontal control-strip scroll behavior for knobs and do not reintroduce knob styling for these actions.</action>
  <acceptance_criteria>
    - `rg -n 'label="Play"|label="Send"|button-text="⌫"|label="Undo"' src/components/keyboard/KeyboardActionBar.vue` returns no matches.
    - `rg -n 'data-testid="live-strip-action-play"|data-testid="live-strip-action-backspace"' src/components/patterns/LiveCard.vue` returns both matches.
    - `rg -n 'data-testid="pattern-send-button"' src/components/patterns/PatternList.vue` returns one match.
    - `bun run test:run src/__tests__/components/keyboard/KeyboardActionBar.test.ts src/__tests__/components/patterns/LiveCard.test.ts src/__tests__/components/patterns/PatternList.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>bun run test:run src/__tests__/components/keyboard/KeyboardActionBar.test.ts src/__tests__/components/patterns/LiveCard.test.ts src/__tests__/components/patterns/PatternList.test.ts</automated>
  </verify>
  <done>Transport/edit/commit controls are visually separated from instrument/configuration controls within the keyboard working area, and the new action surfaces are covered by targeted component tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make delete and send mutations visually legible in the working strip</name>
  <files>src/stores/patterns.ts, src/components/patterns/LiveStrip.vue, src/__tests__/stores/patterns.test.ts</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/01-instrument-trust-flow/01-CONTEXT.md
src/stores/patterns.ts
src/components/patterns/LiveStrip.vue
src/components/patterns/PatternList.vue
src/composables/useLiveStrudelMirror.ts
src/__tests__/stores/patterns.test.ts
  </read_first>
  <behavior>
    - Test 1: `removeLastFromCurrentSketch()` removes live notes before loaded base notes and records enough metadata to identify the affected index after deletion.
    - Test 2: `sendCurrentPattern()` saves the full desk sketch, clears the active line, and records a send/reset mutation for the next sketch.
    - Test 3: `LiveStrip` responds to append, load, delete, and send mutations with explicit scroll/orientation handling instead of only appending to the tail.
  </behavior>
  <action>Per D-08 through D-11, extend `src/stores/patterns.ts` with explicit mutation metadata for the active desk, such as `lastSketchMutation` containing `type`, `affectedIndex`, and `timestamp`, so the UI can distinguish append/load/delete/send events without guessing from note counts. Keep `sendCurrentPattern()` saving the combined sketch, but make it explicitly mark the desk as cleared for the next line per D-11. Keep `removeLastFromCurrentSketch()` preferring live notes before loaded base notes per D-10 and record which side of the desk changed. In `LiveStrip.vue`, replace the current append-only/load-only scroll logic with mutation-aware watchers: scroll to tail on append, reset to start on load/send, and center or keep in view the affected token on delete. Add a brief, grep-visible delete/send feedback hook such as `data-last-mutation` or a class on the strip root so deletion is not silent.</action>
  <acceptance_criteria>
    - `rg -n 'lastSketchMutation|affectedIndex|type: \"delete\"|type: \"send\"' src/stores/patterns.ts` returns matches.
    - `rg -n 'lastSketchMutation|data-last-mutation|mutation' src/components/patterns/LiveStrip.vue` returns matches beyond the existing append/load watchers.
    - `bun run test:run src/__tests__/stores/patterns.test.ts` passes with coverage for send-clear and delete-precedence behavior.
  </acceptance_criteria>
  <verify>
    <automated>bun run test:run src/__tests__/stores/patterns.test.ts</automated>
  </verify>
  <done>The active sketch keeps its desk-like semantics, and delete/send mutations leave a visible orientation trail in the live strip instead of silently mutating off-screen.</done>
</task>

</tasks>

<verification>
- Run the targeted component and store tests from both tasks.
- Run `bun run type-check` after both tasks complete to catch any store/component contract drift.
</verification>

<success_criteria>
- `KeyboardActionBar.vue` keeps instrument/configuration controls distinct from transport/edit/commit actions within the keyboard working area.
- `LiveCard.vue` owns compact playback and backspace actions.
- `PatternList.vue` owns the pattern-level send action.
- `patterns.ts` and `LiveStrip.vue` expose explicit delete/send/load/append mutation handling that keeps the user oriented.
</success_criteria>

<output>
After completion, create `.planning/phases/01-instrument-trust-flow/01-01-SUMMARY.md`
</output>
