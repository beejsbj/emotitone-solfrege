/**
 * Modular Color System
 * Exports the unified color system from the main composable
 */

export { useColorSystem } from "../useColorSystem";
export type { NoteColorRelationships, DynamicColorConfig, MusicalMode } from "@/types";